# 생성물·캐시
output/
.cache/
# ★[8/30] 舊 '**/yt_cache/ · **/hunter_yt/' 제외 규칙 삭제 — 자막이 /tmp에 저장되는데
#    gitignore까지 막고 있어 **두 겹으로** 유실됐다. 자막은 이제 data/transcripts/에 영구 보관한다.
*.vtt
__pycache__/

# 민감정보 — 토스 API 키는 절대 커밋 금지 (애초에 파일로 저장하지 않음)
.env
*secret*
*client_secret*

# OS/에디터
.DS_Store
*.swp

# 스크린샷/렌더 도구 (앱과 무관, 일회성)
node_modules/
package.json
package-lock.json
data/app/trades.jsonl.bak

# 무인 루틴 실행 로그 — 머신 로컬 상태(경로 B). 커밋 대상 아님.
data/logs/

# 의미검색 인덱스 — 파생물(원장에서 언제든 재생성). 크고 바이너리라 커밋 안 함.
data/cache/
