# 데이터 소스 — 전문

> **정본이다.** 2026-09-09 CLAUDE.md 다이어트로 옮겼다 — 내용 무삭제, 위치만 이동.
> CLAUDE.md엔 도구 목록과 ⚠️ 함정만 남기고, 각 소스의 배경·실측·이력은 여기가 정본이다.
> 커버리지·키·폴백의 기계 정본은 `docs/data_coverage.md`가 따로 있다 — 이 문서는 **왜 그 도구가
> 생겼고 무엇에 속았는지**를 담는다.

- **시세·환율·지수**: `market_data.py`(Yahoo, 무키·무의존) 1차. 키 없이 국내+미국+환율 전부 조회.
- **매크로 지표 [7/14 신설]**: `macro_data.py`(FRED 무키 CSV) — 미국채 10Y/2Y·2s10s·실질금리·BEI·EFFR·VIX·WTI·달러지수·CPI YoY·실업률 = **금리·물가 서술의 하드넘버 정본**(1영업일 지연 정상). `--polymarket <키워드>`로 이벤트 내재확률 참고(정본은 CME FedWatch).
- **변동성 게이지 [7/19 신설]**: `vol_gauge.py`(Yahoo OHLC 무키·stdlib) — 실현변동성(RV20)·EWMA(0.94 군집)·**폭풍 점수**(오늘 RV의 최근1년 백분위)·1년 레인지 산출(코스피·코스닥+보유15). 근거 = GARCH(study_log 7/18 ④). **크래시 TF §6 재진입 트랜치 온건 스케일의 입력**(정훈 승인 7/19 — 7,500 이진 안전핀은 하드 플로어 유지, 그 위 폭풍 %ile로 트랜치 사이즈 연속 감산). 측정치일 뿐 안전핀 해제 판정(§5 3중 게이트)을 대체하지 않음.
- **실제 보유수량·현금**: 토스 Open API(`toss_snapshot.py`, **조회 전용**) 또는 계좌 스크린샷이 정본.
- **📑 재무제표 3표 (전종목·1차 출처) [7/29~30 신설 — 정본 = `docs/data_coverage.md`]**: `financials.py`가 보유 14종목(VOO 제외) 손익·재무상태·현금흐름을 **3단 폴백**으로 통합한다. **US = SEC EDGAR XBRL(무키·상장 이래 전 시계열) → Yahoo → FMP** / **KR = Yahoo(무키·23계정) → DART(`DART_API_KEY`·1차 출처) → 네이버 비율**. 두 소스가 1% 초과로 갈리면 `source_conflict` 플래그 + 병기. **재무 플래그**(margin_trend_break=리스크룰 4 메모리 정점 기계화 · inventory_surge · fcf_negative_turn · debt_buildup · receivable_divergence · backlog_growth · dilution) = **측정·경보 전용, 자동 매매 아님**. **펀더 서브스코어(0~100)** = CANSLIM 5축 자동 채점(C·A·마진방향·FCF·재무건전성), N·L·M은 PM 판단. ⚠️ **접미사 정확도**: 티커 접미사가 다르면 다른 회사다(7/30 사고 — 두산로보 `.KQ`가 코스닥 남의 종목을 담고 있었음). `market_data.py` 기준과 항상 일치시킬 것.
- **미국주 TTM 파생(보조)**: FMP API(`fundamentals.py`, 매출·EPS·마진·FCF·PE). 키 = 환경변수 `FMP_API_KEY`(조회 전용). **무료 플랜 = 미국 전용·250회/일 + 심볼 화이트리스트** — MU·VOO·ANET·AVGO·ORCL은 402(NVDA·AAPL·MSFT·GOOGL·META만 정상). **402 종목은 이제 EDGAR가 대체하므로 WebSearch 폴백이 아니다.** [7/30] TSLA는 매도(7/7)로 호출 목록에서 제거.
- **국내 펀더멘털**: ~~FMP 미지원이라 WebSearch~~ → **폐기(7/30)**. 국내도 **Yahoo·DART 하드넘버가 1차**이고, 증권사 리포트(`broker_reports.py` 한경 컨센서스 원문·목표가 이력)가 그 위에 얹힌다. 국내 펀더를 산문으로만 쓰지 말 것.
- **뉴스·수급·컨센서스**: WebSearch/WebFetch로 교차검증. **🌐 [6/16 정훈 지침] 기관·증권사 자료 우선**: 종목 리서치 시 국내 기사보다 **증권사/IB sell-side 리포트(국내 + 글로벌 Goldman·MS·JPM·Citi·UBS 등)와 외신(Bloomberg·Reuters·CNBC·FT·WSJ)**을 폭넓게·유심히. 목표가 레인지·상향/하향 이력·양쪽 논거 명시, 단일 출처 금지. **[7/4] 이 지침의 정본 = `docs/desk_playbook.md` §1**(舊 6개 agent 파일 중복분 단일화) — 전 데스크(현재 9개)가 Tasks 0에서 플레이북(공통 지침 + 캘리브레이션 교훈 + 자기 데스크 누적 교훈)을 Read하고 작업하며, 교훈 갱신은 그 파일 §4 지속 학습 루프(매 보고서 append·주간 self-review 수치 갱신)를 따른다.
- **국내 뉴스·리테일 심리 [7/19 연결]**: `naver_data.py`(네이버 오픈API·NCP API HUB, 조회전용·stdlib). ①**뉴스** = 국내 원문 1차(`--news`, US 리전 WebSearch 보강 — 데이터센터 IP 지역차단 없음, 7/19 실측 200) ②**검색어트렌드** = 리테일 관심·공포 게이지(`--trend`, "공포에 사라" 역발상 신호). 키 = 환경변수 `NAVER_NCP_KEY_ID`/`NAVER_NCP_KEY`(조회전용). 국장 데스크가 Task 2에서 사용, 키 없으면 WebSearch 폴백. ⚠️트렌드는 요청 1콜 내 상대정규화(0~100) → 심리 키워드 단독 그룹 조회. 뉴스 '정황' 기사는 KRX 마감 확정 전 미채택(desk_playbook §3 [7/4]).
- **리테일 심리 게이지 [7/26 신설]**: `naver_sentiment.py`(naver_data 위에 얹은 stdlib 계산기) — 네이버 검색어트렌드 = **한국판 GSVI**(검색량 기반 투자자 관심도. 문헌: 개인 주도 시장일수록 관심 급증이 이후 초과수익을 되돌리는 경향). 산출 ①**심리 3그룹**(공포·항복·유입)의 **1년 %ile** ②보유 국내 5종목 **관심도 %ile**(급증=과열 경계) ③**뉴스 버즈**(24/72h 기사수). `vol_gauge.py` 폭풍 %ile과 **같은 %ile 문법** — 가격 변동성(폭풍)과 심리(공포·항복)가 동시에 90%ile+면 항복 정황으로 읽는다(= "공포에 사라"의 정량 게이지). **측정 전용 — 안전핀·트랜치·별점 어떤 룰도 바꾸지 않는다**(매수 트리거 아님). 1콜에 1년치를 받아 그 안에서 %ile을 뽑으므로 콜 간 비교가 성립. `--save`로 `data/app/sentiment.json` 누적.
- **🩳 국내 수급 4축 — 공매도·대차·신용·투자자별 [8/31 신설 · 1차 출처]**: `toss_flows.py`(토스 Open API·조회 전용). **이 축은 오래 비어 있었다** — KRX가 막아서다. 8/31 로컬 실측으로 원인이 확정됐다: 포털 HTML은 200인데 `getJsonData.cmd`만 **400 `LOGOUT`**(Referer 없으면 403) = **IP가 아니라 세션 가드**라 로컬 이전으로도 안 열린다. 그래서 축을 포기했는데 **토스가 KRX보다 더 많이 준다**는 걸 같은 날 확인했다. **①공매도** 거래량·비중(+20일 평균·**%ile**) **②대차잔고**(기관 간)·체결/상환 **③신용융자 + 신용대주**(개인 공매도) **④투자자별** 개인·외국인·기관 + **기관 7분류**(금융투자·보험·투신·사모·은행·기타금융·연기금). ④는 네이버·뉴스로 긁던 축의 **1차 출처 대체재**다 — "기관 순매도"를 뭉뚱그리면 연기금인지 사모인지 몰라 해석이 안 된다. **첫 수집(8/31)**: 삼성전자 공매도 **9.19%**(20일 평균 7.34%·**80%ile**)·기관 -235.0만주 중 **사모 -79.9만(34%)** / 원익IPS **27.39%(95%ile)** / 두산에너빌리티 대차잔고 2,230만주. ⚠️ **측정 전용** — 안전핀·트랜치·별점 어떤 룰도 안 바꾼다(`vol_gauge`·`naver_sentiment`와 같은 층위·같은 %ile 문법). ⚠️ **T+1** — 당일 행은 장 마감(15:30) 직후 갱신, `updated_at` 확인. ⚠️ **IP 허용목록 필수** — `403 access_denied`는 키 문제가 아니라 공인 IP 변경이다(WTS에서 재등록). 국장 데스크가 Task 2에서 `--save`로 수집한다.
- **📋 국내 수시공시 감시 [7/30 신설]**: `dart_disclosure.py`(DART `DART_API_KEY`·조회전용) — **리스크룰 2 '펀더멘털 훼손'을 판정할 유일한 장치.**
  중대성 3단 분류(🚨critical 훼손·희석·지배구조 / ⚠️notable 계약·자사주·5%보고·실적 / ·routine 숨김)가 본체 —
  삼성 한 종목만 30일 836건이라 분류 없이는 아무도 안 본다. `--insider`(elestock 임원 보유증감 **합산**) ·
  `--major`(majorstock 5% 대량보유 = **국민연금·지주사 지분변동**). **경보 전용 — 자동 매매 아님.**
- **👤 미국 내부자 매매 [7/30 신설]**: `insider_us.py`(SEC Form 4·무키). **재량적 공개시장 매수(P)와 10b5-1 사전약정 매도(S)를 분리** —
  총량만 보면 오독한다. 매도 대부분은 사전약정이라 비관 신호가 아니고, 드문 매수(P)가 신호다.
  **[8/1] `--major` 추가 = 미국 5% 대량보유(13D/13G)** — 국내 `dart_disclosure --major`와 같은 문법. 13D=경영참여(행동주의)/13G=단순투자.
  ⚠️ **발행사 피드엔 방향이 섞인다**(남이 우리를 산 [피취득] vs 우리가 남을 산 [취득]) — 라벨 확인하고 읽을 것.
- **📈 EPS 추정치 리비전 [8/1 신설]**: `eps_revisions.py`(Yahoo `earningsTrend`·무키). 보유+워치 **33종목**(국내 포함, VOO만 ETF 부재).
  목표가 **레벨**(consensus)·**밸류**(value_core)·**확정 실적**(financials)과 다른 **네 번째 축 = 전망의 방향**(추정치 1차 미분).
  90일 변화율 + 상향/하향 브레드스(**표본 축소 n/(n+4)** 적용·표본 수 병기). **점수는 부호 있는 -100~+100 — 별점 0~100 밴드와 다른 축이니 섞지 말 것.**
  ⚠️ **측정 전용 — 별점·스코어·트랜치 어떤 룰도 안 바꾼다**(정훈 8/1 지시). Yahoo가 과거 시계열을 안 줘 **낙폭 구간 층화 검증이 불가**하기 때문.
  `--save`로 원장 누적 중 — 표본 쌓이면 R3에서 층화 검증 후 승격 재논의. 연구 정본 = `docs/research/eps_revision_study_2026-08-01.md`.
- **🗣️ 실적 가이던스 [8/1 신설]**: `guidance.py`(SEC 8-K Item 2.02·무키). 보도자료 EX-99에서 **다음 분기 가이던스 수치**(매출·마진·EPS·캐펙스)와 경영진 코멘트 추출.
  **§10-2 메모리 정점 판정(마진 추세)의 입력** — MU FQ4 가이던스 GM **약 86%**(FQ3 84.9%에서 추가 상승 = 아직 정점 신호 아님).
- **🎙️ 어닝콜 트랜스크립트 전문 [8/1 신설]**: `transcripts.py`(earningscalls.dev·**무키·무가입**). 애널리스트 Q&A 포함 전문(3.6만~6.7만자, 9/9 종목).
  보도자료에 수치를 안 내던 **AAPL·MSFT·GOOGL·ORCL 사각지대를 메운다**(매수 1순위 GOOGL 포함). `--grep`으로 수요·공급·재고·캐펙스 발언만 추출.
  **§10-2 메모리 정점 판정의 선행 신호** — 마진이 꺾이기 전에 수요 코멘트가 먼저 바뀐다(예: MU FQ3 "demand continues to be well above our supply").
  ⚠️ **3자 집계 사이트 — 수치 인용 시 8-K·IR 1차 출처와 교차 필수.**
- **🔎 공시 전문검색·8-K 이벤트 [8/5 신설]**: `edgar_search.py`(SEC EDGAR FTS·무키). 기존 EDGAR 도구는 전부 **정해진 XBRL 태그에서 숫자만** 꺼냈다 → **공시 본문(산문)에서 문구를 찾는 축**이 없었고, 그게 7/29 CXMT형 *2차 매체 받아쓰기*의 구조적 원인이었다. `--q "문구"`로 2001년 이후 전 공시 본문 검색(첫 수확: `HBM4`가 **MU 5건·NVDA/AVGO 0건**), `--events`로 8-K **item 코드** 스트림(2.02 실적·1.01 중대계약·3.02 희석 등 중대성 3단 분류 — `dart_disclosure`와 같은 문법). ⚠️ **탐색 전용** — 히트는 "그 문구가 있다"는 사실일 뿐이니 인용 전 원문 URL로 문장을 직접 확인할 것(7/26·7/28 정정룰). ⚠️ 서버가 **다중 CIK·무필터 질의에 500**을 주므로 회사 단위로 쪼개 호출한다.
- **📊 횡단면 상대비교 [8/5 신설]**: `peer_compare.py`(financials.json 재사용·무네트워크). `financials.py`가 종목을 **세로로**(시계열) 본다면 이건 **가로로**(동종 대비) 본다 — 그동안 "현대차 스코어 25.8이 업종 특성인가 회사 문제인가"를 우리 숫자로 못 갈라 sell-side의 '업종 대비' 서술을 받아썼다. 피어 그룹 = **데스크 3축 그대로**(반도체·빅테크·전력). **두 렌즈를 같이** 낸다: 피어 백분위 ↔ 자기이력 백분위, **둘이 갈릴 때가 정보**(현대차 = 피어 1/3위인데 자기이력 12%ile = 업종이 아니라 자기 하락). ⚠️ **비율만 비교**(통화 혼재로 절대금액 비교는 무의미) · n=3~6이라 백분위는 사실상 순위 · **측정 전용**.
- **🧠 기억 통합 회수 [8/5 신설]**: `memory_recall.py`(원장 4종·무네트워크). 우리 기억은 `decisions`·`calls_log`·`missed_moves`·`rule_log` **네 군데에 흩어져 서로를 몰랐다** — 쌓는 쪽은 갖췄으나 **끌어올리는 쪽이 없었다**(8/2 확인된 "산문 8~9회 vs 오더 0회" 구멍의 인지적 원인). 금융 에이전트 문헌(FinMem·FinAgent)의 **계층 기억 + 회수**를 우리 원장에 맞춘 것: 점수 = **관련성 × 최신성(반감기 90일) × 원장가중**(열린 아젠다 > 종결 결정 > 검증 미스무브 > 일반 콜). 티커↔한글명 양방향(`현대차`·`005380.KS` 둘 다 걸림). ⚠️ **읽기 전용** — 점수는 **읽을 순서일 뿐 옳았다는 뜻이 아니다**. 같은 종목의 **틀린 콜·기각된 대안도 같이** 끌어올린다(확증편향 방지).
- **🔎 기억 의미검색 [9/1 신설 · 정훈 승인 "더 정확한 거로"]**: `memory_embed.py`(BAAI/bge-m3·1024차원·로컬 CPU). 위 회수기는 **어휘 일치**라 *"메모리 정점을 언제 판정하기로 했더라"* 처럼 **질의어가 안 박힌 기록을 원리적으로 못 찾았다**. 인덱스 대상 = **산문만 3,797청크**(decisions 180·missed 44·hunter 656·보고서 2,917) — **calls_log·rule_log는 의도적 제외**(티커·별점·목표가 같은 수치 스냅샷이라 의미검색에 걸면 노이즈만 는다. 그쪽은 구조적 매칭이 이미 정확하다). `memory_recall.py --semantic`이 하이브리드로 쓴다(**대체가 아니라 보강** — 티커 정확매칭은 문자열 쪽이 더 낫다). ⚠️ **랭킹 전용 — 읽을 순서만 바꾼다.** 룰·별점·트랜치 어떤 판정도 안 건드린다. ⚠️ **반대 증거 슬롯(`--contra`, 기본 2)** — 의미검색은 질의와 *닮은 것*만 올리는 성질이 있어 놔두면 8/5 원설계(*틀린 콜·기각된 대안도 같이 끌어올린다*)를 깨뜨린다 ⇒ 기각안 있는 결정·verdict=miss 미스무브·채널 [정정] 건의 자리를 **강제 확보**한다(contra 후보 223건). ⚠️ **인덱스는 파생물이라 조용히 낡는다** — 원장이 늘어도 안 따라오는데 회수는 성공한 것처럼 보이고 **빠지는 게 하필 최근 기억**이다 ⇒ `check_memory_index()`가 WARN(가드 자가검증 등록됨), R3가 주간 재생성. ⚠️ **의존성 정책 예외** — 이 레포 첫 비-stdlib 의존(torch CPU + sentence-transformers ~1GB + 모델 2.2GB). 포터빌리티가 전제였는데 로컬 확정으로 그 전제가 풀렸다. **파이프라인 코어는 stdlib 유지**, 이건 분석 보조 레이어에만.
- **🧾 체결 원장 [8/23 신설 — 정본 = `data/app/trades.jsonl`]**: `trades.py`가 원장을 이동평균법으로 재생해 **수량·평단·실현손익을 파생**시키고 `portfolio.json`과 대사한다. **설계 반전**: 舊 구조는 수량·평단이 정본이고 체결은 산문이라 체결 때마다 사람이 세 곳을 손으로 맞췄고 **두 번 어긋났다**(8/6 GOOGL 1주일·8/19 ANET·VOO 8일). 이제 **원장이 정본**이라 파생물은 어긋날 수 없고, 남는 위험(기입 누락)은 수량 불일치로 드러나 `validate_report.check_trade_ledger()`가 **FAIL**로 잡는다(음성 테스트 통과). 체결 기입 = `trades.py --add ... --source "<스크린샷 근거>"` (source 필수). **첫 재생: 체결 12건 전 종목 일치.** ★★[8/31 전면 재구성 — 토스 Open API 연결] 로컬 이전 후 토스 Open API가 열리면서 **체결 286건(2024-07-08~2026-08-27) 원본**이 들어왔다. `toss_import.py`가 원장을 통째로 재구성했고(舊 opening·closed 행은 폐기 — 새 체결이 전 구간을 덮어 같이 두면 실현손익이 **이중 계상**된다), **검증 게이트 = 재생 결과 vs 토스 보유 API 대조**에서 **14종목 수량·평단 전량 일치**(미국주 원가 합계 $3,213.01이 토스 보고값 3213.008006과 정확히 일치). ⇒ **성과 수치가 전면 갱신됐다**: **데스크 이전 45건 승률 66.7%·+156,044원 / 이후 7건 승률 71.4%·+106,357원**(누계 +262,401원). 舊 표기(이전 46건 67.4%·+163,796 / 이후 6건 83.3%·+183,577, 누계 +347,373)는 **스크린샷 복원분 기준이라 폐기**. 손익비 = 이전 미국주 39건 **0.75**(평균이익 +13.8% vs 평균손실 -18.4% — 작게 이기고 크게 지는 처분효과 재확인) · 이전 국내주 6건 3.06 / 이후 미국주 7건 5.15(⚠️**표본 7건, 손실 실현 2건뿐이라 판정 아님**). ⚠️ **잔차 2건 명시**: AAPL +0.027960주·TSLA +0.007012주가 주문이력으로 설명되지 않아(체결 합이 음수) `side:"opening"`으로 분리 기록했다 — 토스 주식 선물·이벤트 등 **주문 아닌 취득 경로**로 추정. symbol 필터 재조회로 페이징 누락이 아님을 확인했고, **체결(buy)로 위장하지 않는다**(없는 취득일·취득가를 만드는 것). ⚠️ **`era`는 이제 날짜로 판정**한다 — 舊 구현은 행 종류(closed=pre / sell=desk)로 하드코딩돼 있어 2024·2025년 매도 45건이 전부 '데스크 이후'로 둔갑했다(데스크 성과 3배 부풀림). **데이터 모양이 바뀌면 모양에 기댄 라벨은 조용히 거짓말을 한다.** ★[8/23 확장] 정훈이 토스 **실현손익 화면** 5장을 주면서 **데스크 이전 청산 31건**(2026-02-23~06-24)이 `side:"closed"`로 편입됐다 — 화면이 주수·단가를 안 주므로 체결로 위장하지 않고 실현액·수익률·역산 취득원가만 기록한다. **검증**: MU 6/24가 우리 원장 +$6.50(43.4%) vs 토스 +$6.49(43.3%)로 센트 단위 일치 → 산문에서 복원한 원장이 독립 검증됐다. **성과는 데스크 전/후를 갈라서 본다**(합치면 우리 판단의 성과가 그 이전과 섞여 자기 채점이 불가능해진다): **이전 46건 승률 67.4%·+163,796원 / 이후 6건 승률 83.3%·+183,577원** (누계 +347,373원). 분해 = **미국주 40건 +$114.93(승률 72%·손익비 0.81)** / **국내주 6건 -9,668원(승률 33%·손익비 3.05)**. ★[8/24] **손익비·기대값은 이제 `trades.py`가 자동 산출**한다(`_payoff()` — era×통화별). 舊엔 이 수치가 산문에만 있어 갱신이 안 됐고, 손익비를 재는 스크립트가 83개 중 0개였다(외부 리포 검토 중 발견 — `docs/research/repo_review_2026-08-24.md`). **측정 전용.** ⚠️ **손익비가 통화별로 정반대다** — 미국주는 자주 이기지만(72%) **작게 이기고 크게 진다**(이긴 건 +14.9% vs 진 건 -18.4% = 처분효과의 실측 증거, 경제사냥꾼 8/21 영상 '옳은 건 빨리 팔고 내린 건 붙든다'와 일치). 국내주는 드물게 이기지만 크게 이긴다(+36.7% vs -12.0%) — 단 **6건이라 단정 금지**. 최대 손실 = 와이지엔터 -189,638(-26.8%) · **SEALSQ -177,277(-48.7%, 2025-01-24)**. ⚠️ **데스크 이후 6건을 우월 근거로 쓰지 말 것** — 표본 6건이고 전부 의도적 익절이라 선택편향이다. ⚠️ **실현손익 구간은 완전하다**(리스트 끝까지 확인 — 마지막이 '25년 1월 24일' 표기, 2025-01-24~2026-01-19 사이엔 청산 없음). 단 **현재 보유분의 취득일·취득환율은 여전히 미상** — 그건 이 화면이 주지 않는다. ⚠️ 6/13 이전은 `side:"opening"` 기초 잔고로 명시 — 체결로 위장하지 않는다.
- **💱 통화 익스포저 [8/23 신설 — roadmap P0 2-3 해소]**: `fx_exposure.py` — 통화별 비중(주식+현금)·원/달러 1y·3y·5y %ile·환율 1% 민감도·**환손익 3분해**(종목/환율/교차). 달러가 7할인 포트에서 **환율이 종목보다 큰 단일 변수**인데 그동안 측정 축이 없었다. 첫 산출 = 달러 **71.9%** · 1% 변동 **58,745원** · 미국주 **종목 +165,976 vs 환 -277,477** (주가로 번 걸 환율이 되돌리는 중). ★[8/31 해소] 취득환율이 **실측으로 교체**됐다 — `us_avg_fx_cost` 역산 1,467.4 → **체결 실측 1,460.9**(보유 9종목 매수를 체결일 원/달러 종가로 가중평균), **환율 커버리지 13.3% → 99.9%**. 재산출: 달러 **73.5%** · 1% 변동 **59,790원** · 미국주 종목 +391,358 vs 환 **-271,887** 교차 -22,669. ⚠️ 여전히 근사다 — 체결일 *시장 종가*이지 토스가 실제 적용한 환율(스프레드·체결시각 반영)이 아니다.
- **🔗 동조·상관 통계 [8/23 신설 — 정훈 지시 "골드만삭스 깃헙 코드 참고"]**: `portfolio_stats.py` — `goldmansachs/gs-quant`(Apache-2.0 v2.1.4) `timeseries/econometrics.py`의 정의를 **stdlib로 재구현**(gs-quant는 pandas/numpy 의존이라 import 불가 — 수식만 이식, 코드 복사 아님). 산출 = 연환산 변동성(sample std×√252)·**상관행렬**·베타(Cov/Var)·최대낙폭·**실효 분산 종목수**. **왜 필요했나**: 우리는 집중도를 *비중과 사람이 붙인 섹터 라벨*로만 재왔고 risk-desk 지시문의 *"memory-bet overlap(삼성·NVDA·MU·AVGO)"*은 **눈대중**이었다 — 라벨이 '빅테크'와 '반도체'로 갈려도 같이 떨어지면 분산이 아니다. **첫 산출(60거래일)**: 보유 14종목이 **비중 기준 7.41종목어치 → 상관 기준 2.28종목어치**(분산의 **69%가 동조로 소멸**), 포트 변동성 36.3%(상관 무시 시 54.7%), 최상위 상관쌍 LG전자↔두산로보 0.80·삼성전자↔현대차 0.70·NVDA↔VOO 0.69. 베타 = 코스피 0.39·S&P500 **1.86**·필반 0.30. ⚠️ **변동성은 각 심볼의 자기 거래일**, 상관·베타는 공통 거래일 교집합으로 잰다(KR·US 거래일이 어긋난 채 √252를 곱하면 변동성이 약 1.14배 부풀려진다 — 실측 후 분리). ⚠️ **합성 시계열**(현재 비중을 과거에 고정)이라 실제 계좌 수익률이 아니다 — 실제 트랙레코드는 체결 원장이 6/13 이전까지 채워져야 나온다. ⚠️ **측정 전용.**
- **🧭 포트폴리오 리스크 게이지 [8/23 신설]**: `portfolio_risk.py` — **8축**(종목집중·**동조(상관)**·통화·테마·⭐2이하·하락종목·현금·국면) 가중합 **0~100**. ★[8/23] 동조 축 신설 — 舊 7축은 집중도를 라벨로만 봐서 *진짜 분산 여부*를 못 쟀다. 신설 직후 **동조가 최대 기여 축**(78·기여 11.0)으로 올라오며 점수 35→41. 종목 집중은 0인데 **같이 움직이는 게 진짜 리스크**였다 + 규칙 기반 인사이트. 종목 별점과 **다른 층위**(조합의 성질)다. ⚠️ **측정 전용 — 점수가 높다고 매도 신호가 아니다**(룰이 상위 판정자). 미측정 축은 0으로 채우지 않고 **분모에서 제외**한다(8/22 "가드 없는 폴백은 침묵보다 나쁘다"). 셋 다 `build_app_data` → 앱(홈 리스크·통화 카드 / `#trades` 화면)에 노출되고 risk-desk Task 4·5가 읽는다.
- **🖥️ 웹 화면 판독 [8/22 신설]**: `web_shot.py`(Playwright+Chromium·무키) — 텍스트·API가 **닿지 못하는 화면**(렌더링된 표·차트·이미지형 공시)을 PNG로 찍어 에이전트가 눈으로 읽는다. 착안 = 정훈이 가져온 claude-video 와치의 문제의식 *"에이전트가 화면을 못 본다"*(도구 자체는 이 환경에서 불가 판정 → 문제의식만 우리 형태로 구현). `--naver <코드>` 프리셋 · `--selector`로 영역만 크롭(권장). **실측**: 네이버금융 종목화면에서 시세·PER·외국인소진율·목표주가·52주 고저 전부 판독. ⚠️ **유튜브 영상 프레임은 불가**(watch=reCAPTCHA, 스트림=5개 클라이언트 봇차단) — 열리는 건 썸네일 1장뿐이고 이 갭은 로컬 이전(1단계 8/31)까지 열려 있다. ⚠️ **측정·판독 보조 전용**(vol_gauge·chart_read와 같은 층위 — 어떤 룰도 안 바꾼다) + **토큰 규율**: 1장 ≈1,000~1,600토큰이라 전종목 상시 캡처 금지, 텍스트 경로 실패 시나 눈으로 봐야 판단이 갈릴 때만.
- **🎞️ 유튜브 화면 [8/22 신설 · ★9/1 고해상도 배선]**: `yt_frames.py` — 정훈 지시 *"화면이랑 자막 둘 다 하면 정보를 더 깊게 알 거 아니야"*의 실현분. **8/31 로컬 이전으로 yt-dlp 스트림이 열리면서 8/22에 못 하던 정공법이 살아났다**(舊 웹 환경: 5클라이언트 봇차단 · Playwright watch=reCAPTCHA · googlevideo는 URL이 발급 IP에 바인딩돼 403).
  **①고해상도(주 경로) = `--hires --at <시각>`** — yt-dlp 스트림 + ffmpeg로 **그 시점 1장**을 720p로 뽑는다(3~4초). 자막이 수치·차트를 말한 지점을 집어 검증하는 것이 정석. `--hires --count N`은 영상 전체 균등분할.
  **②스토리보드(값싼 훑어보기) = `--sheets`** — 플레이어 스크럽 미리보기(L2 160×90·25프레임/시트, CDN이라 캡차 없음). 여전히 유효하나 **차트 축·표 숫자는 못 읽는다**.
  ★**쇼츠가 이제 읽힌다** — 스토리보드는 쇼츠 타일이 50×90이라 판독 불가였고 채널 업로드 상당수가 쇼츠였다. 720×1280 실측에서 삽화 속 작은 영문 표제까지 판독.
  ⚠️ **장면전환(scene) 추출은 실측으로 기각(9/1)** — ffmpeg 9 `scdet`으로 이 채널 영상의 컷 점수를 재니 **0.000~0.127**(threshold 0.01에서도 0장). 연속 애니메이션에 자막만 바뀌어 **자를 컷이 없다** → 균등분할이 답. *구현 전에 재봤기에 '돌지만 아무것도 못 잡는' 기능을 안 만들었다.*
  ⚠️ **해상도는 `-S res:N`(짧은 변 기준)으로 고른다** — `height<=720`은 세로 1080×1920 영상에서 1920을 걸러내 **360×640을 집어온다**(9/1 실측).
  ⚠️ **화면은 자막의 보조 축이고 그 자체로 2차 출처다** — 영상 내용의 정본은 `hunter_latest --fetch` 자막이고, 수치는 1차 출처와 교차한다. ⚠️ **토큰 규율**: 고해상도 1장 ≈1,500~2,500토큰(스토리보드 시트 1장 ≈1,100~1,600). R1이 하루 7~8편을 도니 **전편 상시 캡처 금지** — 상한 1회 2편·편당 2장, 기본 `--count 4`·하드캡 12.

- **🟡 FMP는 심볼 5개짜리 [8/1 실측] — 유료 전환 금지**: 레거시 `/api/v3/*`는 전면 차단(`Legacy Endpoint`)됐으나
  `fundamentals.py`는 **이미 `/stable`을 써서 정상 작동**한다(초판에 "죽었다"고 쓴 건 오기·정정). 다만 stable도 **심볼 화이트리스트**라
  실사용 가능 종목은 **NVDA·AAPL·MSFT·GOOGL·META 5종뿐**(MU·ANET·AVGO·ORCL·국내 전부 402).
  FMP가 주던 건 이미 EDGAR·DART·Yahoo가 전부 덮는다 → **돈 쓰면 가진 걸 다시 사는 것**. 정본 = `docs/data_coverage.md §5c`.
  **교훈: 외부 API는 말없이 죽는다** — 문서의 "✅ 정상"은 마지막 확인일의 사실일 뿐, 키 붙은 레이어는 주기적 실호출로 생존 확인할 것.
- **🚫 DRAM 실물가 — FRED 반도체 PPI를 프록시로 쓰지 말 것 [8/1 확정]**: 무키로 잘 돌지만 메모리 급등기에 **오히려 하락**한다
  (`PCU334413334413` 2026-04 30.6 → 06 **29.7**). 로직·아날로그 혼합 집계 + 2개월 지연. 쓰면 **7/29 CXMT 오류의 자동화**다.
  무료 기계판독 DRAM 피드는 **없다**(TrendForce 유료) → TrendForce 무료 주간 스팟 기사·국내 언론 고정거래가로 수동 조회.
  **메모리 사이클 서술 시 실물 호가 1차 출처 확인 여부를 밝히고, 못 했으면 "실물가 미확인"이라고 쓴다.** 정본 = `docs/data_coverage.md §3b`.
- **📐 낙폭·회복·기저율 [7/30 신설]**: `drawdown_history.py` — 낙폭 생애주기(고점→저점→회복)·현재 낙폭의 역대 순위·
  **조건부 기저율**(지금만큼 빠졌던 과거 시점들의 1/3/6/12개월 분포)·매크로 오버레이(VIX·유가·환율·금리).
  ⚠️ **기저율 사용 규율**: ①표본 수 항상 병기(두산로보는 표본 2개 = 통계 아님) ②낙폭 깊이만으로 반등 기대 금지
  (MU -39%의 12M 중앙 +11%·승률 58%) ③"승률 97%"를 "97% 확률"로 쓰지 말 것(표본 중복·생존편향).
- **🎬 경제사냥꾼 영상 = 시장 시야 + 최근 이슈 핵심 소스 (정훈 강조)**: 둘 다 중요 — ①**누적 축적**: 매 영상 분석을 **`docs/research/hunter_log.md`**에 시계열로 쌓아 채널 트랙레코드·반복 논지·시장 흐름의 '시야'를 형성(옛 분석 유지). ②**신규 분석**: 신규 영상을 research-feed로 새로 탐색해 최근 종목별 이슈 반영. 모든 주장 [검증/정정/**대립·미결**/미확인] **4단계**(★8/27 신설 — **[대립·미결]** = 채널 주장과 다른 전망이 맞서지만 **결과가 아직 안 나온 것**. 이때 [정정]을 붙이면 컨센서스를 사실로 취급하는 오류다 — 8/27 금통위에서 실제로 났고, 채널이 맞았다. **[정정]은 사실 확정 후에만.**) 분류, 수치 교차검증(채널은 **방향성은 좋으나 구체 수치 자막오류·과장 잦음** → 방향성 채택·숫자 검증).
  - **⚡ [7/11 토큰 절감 — 영상 분리]**: 3채널 영상 자막 분석은 이제 **메인 보고서가 인라인으로 안 돌린다.** **R1 영상 프리페치 루틴(평일 10:00, 별도 5시간 리셋 창)**이 미리 3채널을 분석해 hunter_log·feeds_log·hunter.json·feeds.json에 캐시하고, **메인 보고서(R2 16:00)는 오늘자 캐시만 소비**(자막 재추출 금지, SKILL §2c 신선도 가드·routines.md). 5시간 롤링 리셋에서 두 무거운 세션이 같은 창에 겹쳐 예산 고갈되는 걸 막는 구조. **볼륨(실측): 경제사냥꾼 일 7~8편·저녁(19~23시) 몰림 → R1 `--max 10`이 전날저녁+오늘아침을 잡고, 그날 오후분은 R2 델타(RSS로 신규 ID만), 그날 저녁 배치는 다음날 R1이 커버.**
  - **⚠️ [6/28 정훈 지시 — 미확인 무조건 다 확인]**: [미확인] 방치 금지. 태깅 전 추가 WebSearch 2~3회로 끝까지 교차검증 → 확인되면 [검증/정정] 승격(예: 애플 가격인상 → CNBC·Bloomberg 다출처 [검증]), 라이브 수치는 범위추정, 끝내 불가면 "N개 출처 시도" 명시. 매 보고서 미확인 개수 최소화.
  - **🎯 [6/28 정훈 지시 — 정리만 말고 때 되면 매수/매도]**: 채널 추천 종목은 단순 로깅이 아니라 **조건 트래커**(`hunter.json` `setups`)로 누적 — 채널이 말한 발동조건(예 외인 하닉 매도중단+환율 1,500 꺾임)을 체크리스트로 분해, **조건 ~75%+ 충족 & 가격존 진입 시 지정가 매수/매도 발동**(§7c). 영상 논지를 계속 기억하다 때가 되면 실제 액션.
    - **🔁 살아있는 추적**: 매 보고서 모든 신규 영상 빠짐없이 — 조건/논지 변경 시 갱신, 새 종목·이슈 전환 시 새 setup 추가·옛것 완료/폐기. stale 방치 금지.
    - **🧭 신뢰-견제 균형 [6/28 정훈 지시]**: 정훈은 경제사냥꾼을 **꽤 신뢰하고 싶어함** → 아이디어를 가볍게 무시 말고 진지하게 비중 채택·추적. **단 PM은 과신 편향을 객관적으로 견제하는 브레이크**: 채널 단독 근거 매수 금지, 기관 sell-side·외신으로 교차검증, 틀린 콜·과장은 분명히 짚고(트랙레코드 정직), 검증된 펀더가 어긋나면 "신뢰해도 이건 신중" 의견. 맹신도 무시도 아닌 중간을 PM이 잡는다.
- **🔑 [8/12 신설 → 8/23 키 발급·가동 확인] YouTube Data API v3 (`YOUTUBE_API_KEY`) — 영상 탐색의 0차 경로.** `hunter_latest.py --after/--before`로 **날짜 구간 전량 열거**(쇼츠 포함)가 되는 유일한 경로다. 필요한 이유 2가지: ①**안정성** — 8/12에 RSS가 3채널 동시 404 + yt-dlp 동시 실패로 무인 R1이 조용히 죽을 뻔했다(현재 폴백 3단: RSS→yt-dlp→채널페이지 스크레이프) ②**과거 소급** — 스크레이프는 최근 30편만 보여서 6주 전 영상을 못 찾는다(8/12에 7/1 영상 3건을 끝내 특정 못 해 `미채점(원본 특정 실패)`로 남김). **무료**(일 10,000 units·search 1회 100 units = 하루 100회). 발급 = Google Cloud Console → YouTube Data API v3 사용설정 → API 키. ★[8/23 실측] **키가 실제로 세팅돼 있고 API 경로가 동작한다** — `--catchup`으로 8/21~22 누락 12편을 정확히 열거해 회수했다(RSS·스크레이프 폴백으로는 '아카이브 최신 이후 전량'을 못 센다). 문서의 '키 대기' 표기가 stale이었다 = **문서의 '✅/대기'는 마지막 확인일의 사실일 뿐**(8/1 FMP 교훈과 동류). ⚠️ 자막 추출은 별개 경로라 여전히 **429 레이트리밋**을 맞는다 — 12편 연속 요청 시 2편 후 차단됐고 **편당 40~90초 페이싱**으로 12/12 전량 회수했다(회피 서사 금지, 페이싱으로 해결). ⚠️ **셸 `sleep`은 이 환경에서 차단**되므로 페이싱 루프는 파이썬 `time.sleep`으로 짤 것. ⚠️ 키가 없으면 `discover_api()`가 빈 리스트를 반환하고 기존 폴백이 그대로 동작 — 지금도 정상 작동하며, 키는 안정성·소급 탐색을 위한 보강이다.
- **📡 [7/7 정훈 지시] 외부 채널 2개 정기 추적 편입 — 수페TV·지식인사이드** (채널별 영상 1편 전문 시청 후 판단, **자산제곱은 미편입** — 원칙론 위주·종목 콜/검증 가능 수치 부재): 탐색·자막 = `hunter_latest.py --channel supe|jisik`(지식인사이드는 투자 키워드 제목필터 내장), research-feed가 3채널 모두 커버. **정본 = `docs/research/feeds_log.md` + `data/app/feeds.json`** — 경제사냥꾼(hunter.*)과 트랙레코드 절대 안 섞는다. 태깅·미확인 최소화·조건 트래커(setups)·신뢰-견제 균형 규칙은 경제사냥꾼과 동일 적용. 지식인사이드는 인터뷰 채널이라 **게스트명 필수 기록**(게스트별 성향 갈림), setups는 명확한 조건부 콜일 때만.
- 단일 출처 의심 수치는 항상 교차검증. 불확실하면 **"미확인"** 명시. 추측 금지.

