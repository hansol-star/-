# 🔧 개발 핸드오프 (다음 세션이 이어받는 메모)

> 이건 **투자 보고서가 아니라 데스크 시스템(코드·연동·룰) 작업**의 세션 간 인수인계 메모다.
> 새 세션은 이 대화 기억이 없으니 여기부터 읽고 이어간다. 완료 항목은 체크·삭제, 새 항목은 append.
> (투자 상태·시세·오더는 여전히 `docs/reports` 최신 STATE SNAPSHOT + `tasks.json`이 정본)

**최종 갱신: 2026-07-30(목) 23:35 KST · 세션 = "재무제표 → 데이터레이어 → 낙폭연구 → 룰 전면개정" (대화형·정훈 동석)**
**git: 오늘 커밋 11개 전부 main 푸시 완료 · 워킹트리 clean · selfcheck GATE PASS**

---

## 📌 다음 세션 첫 단계 (순서대로)

1. **날짜·시세 앵커** — SessionStart 훅이 KST 날짜를 주입한다. `market_data.py`로 실시세 교차검증(장중 등락=개장 / 종가 고정=휴장).
2. **env 키 확인**:
   ```bash
   for k in FMP_API_KEY NAVER_NCP_KEY_ID NAVER_NCP_KEY DART_API_KEY; do [ -n "${!k}" ] && echo "$k 있음" || echo "$k 없음"; done
   ```
3. **이 문서 §열린 작업**을 읽고 우선순위 확인. **§자기경계는 반드시 읽을 것.**
4. **게이트**: dev 작업 = `selfcheck.py` GATE PASS / 보고서 = `validate_report.py` FAIL 0.
5. **연속성**: 완료 시 `git push origin HEAD:main HEAD:claude/<branch>` (정훈 상시 승인분, 묻지 않음).

---

## ✅ 2026-07-30 세션 완료·푸시 (커밋 11개)

### 신설 스크립트 6종
| 스크립트 | 무엇 | 첫 성과 |
|---|---|---|
| `dart_disclosure.py` | 국내 수시공시(중대성 3단 분류 + elestock 내부자 합산 + majorstock 5%보고) | **NAVER 엔비디아 유상증자 적발** |
| `insider_us.py` | 미국 Form 4(재량적 매수 P와 10b5-1 매도 S 분리) | 90일 재량적 매수 **단 1건** |
| `drawdown_history.py` | 낙폭 생애주기·역대순위·조건부 기저율·매크로 오버레이 | 29년 구조 사건 판정 |
| `tranche_rules.py` | 개정 룰 1·2 판정기 | 사다리·훼손 기계화 |
| `rule_tracker.py` | 룰 자기검증 원장 + 11지수 백테스트 | 폭풍 감산 역효과 발견 |
| `capitulation_validate.py` | 항복 가산 2경로 독립 검증 | 현행 유지 확정 |

### 룰 전면 개정 (정훈 승인 "룰도 다 바꾸자" → "해금 구간 감산 제거하자")
- **룰1**: 코스피 7,500 이진 안전핀 **폐기** → **낙폭 사다리**(D1 -25%:15% / D2 -35%:20% / D3 -45%:25% / D4 -55%:25% / 예비 15%)
  - 승수 = **항복 가산만** [1.00, 1.20]. **폭풍 금액 감산 폐지**(11지수 17,946표본에서 해금 4단계 전부 역효과)
  - 폭풍은 **분할 횟수**로만(≥97 4분할 / 90~97 3분할 / <90 2분할, 총액 불변)
  - **하드 플로어 = S&P500 폭풍 ≥70%ile(글로벌 확산)** → 사다리 전면 정지
  - **오늘 상한: 0원 → 112,975 → 282,438원**
- **룰2**: LG전자 전용·이벤트형 → **전 종목·추세형 3중**(마진 3년↓·FCF 악화·순부채↑)
  - 3/3 훼손 착수 / 2/3 감시상향 / ≤1 정상. **금융 자회사 예외**(현대차 = 마진 조건만)
  - 첫 판정: **두산로보 3/3 훼손 착수** · 현대차 마진 훼손(1/1) · 나머지 정상

### 정본 문서 전면 개정
`CLAUDE.md` · `crash_tf.md`(§0 프레임·§2b 사다리·§5b 폐기·§5c 역사좌표·§8 추세형) · `master.md`(§3 룰·§9 결정 6건) ·
`holdings_outlook.md`(한 달 stale → 전면 재작성) · `desk_playbook.md`(§2 규율 4건) · `data_coverage.md`(§4b 사고표·§5b 요청) ·
`self-review SKILL`(§7 역량감사·§8 룰추적) · `routines.md`(R3 배선) · 데스크 4종(semi/bigtech/power/risk/us)

### 교정한 사고 6건
- **두산로보 티커 `.KQ` 오염** — 재무제표에 코스닥 남의 회사. "커버리지 14/14"가 빈 껍데기였다
- **Form 4 XSL 경로** — HTML 파싱으로 전 종목 "0건"
- **수집 실패를 "0건"으로 보고** — 실패와 부재를 구분하도록 수정
- **지수 파일명 규칙 불일치** — 코스피·나스닥·S&P가 통째로 "캐시 없음"
- **`score()`가 전방수익률을 항상 코스피로 계산** — 풀링 후 그대로였으면 표 전체가 무의미
- **NAVER 유상증자 대상 미확인 방치** — 검색 한 번이면 풀렸다(정훈 지적)

---

## 🔓 열린 작업 (우선순위 순)

### A. 즉시 (7/31 금)
1. **v65 보고서** — F4 레버리지ETF **시행일** + AAPL 실적(KST 05~06시) + 한화에어로 실적 +
   **외인 2일째 여부 = 게이트② 첫 점등 판정**. ⚠️ 새 거래일이므로 **v65 새 번호**(v64 부록에 덧붙이지 말 것).
2. **GOOGL 1차 체결 확인** (룰8 — 체결 전 완료 처리 금지).
3. **사다리 상한 282,438원 집행 여부** — 7/31 폰창(17:30~)에서 **F4 변동성 확인 후** 판단.
   국내는 정수 1주 제약(삼성 207,000)이라 실질 대상은 **미국 소수점 트랙**. 4분할 권고(1회 ≈ 70,610원).
   해제 시 우선순위 = **GOOGL → NAVER → MU → META**.

### B. 딥다이브 부채 (룰2 판정에서 발생)
4. **현대차 제조 수익성 딥다이브** — 영업마진 9.3→8.1→6.2%. **금융부문 제외 제조 마진 분리**가 핵심.
5. **두산로보틱스 3/3 훼손 착수** — 이미 ⭐2 모멘텀이라 코어 아님. 트림 검토 여부만 판단.

### C. 검증 부채 (rule_tracker가 추적)
6. **🔴 하드 플로어(S&P ≥70%ile) 미검증** — 발동 표본 0건, 백테스트에서도 미적용.
   **감산 폐지로 유일하게 남은 하방 장치**라 최우선. 검증 방법 설계 필요.
7. **실시간 원장 표본 1개** (40 필요) — 매 보고서·R3에서 `rule_tracker.py --snapshot` 누적.
8. **룰2 훼손 판정 종목 후행 추적** — 두산로보·현대차 이후 주가로 경보 적중 채점(`--score` ⑤).

### D. 남은 데이터 결손 (`data_coverage.md` §3)
9. 어닝콜 트랜스크립트·가이던스(#2) — 메모리 정점 판정의 핵심 입력. 무료 소스 조사
10. EPS 추정치 리비전(#3) — `broker_reports.py` 누적으로 근사 가능
11. 산업 실물 데이터(#4) — DRAM 현물·고정거래가. **7/29 CXMT 오류의 근본 원인**. TrendForce 유료
12. 개별기업 CDS(#5) · 글로벌 IB 리포트(#6) · 미국 13D/13G(#7b)

### E. 정훈 요청 대기 (막힌 게 아니라 콘솔 토글)
13. **네이버 API 4종 활성화** — `cafearticle`·`kin`·`blog`·`webkr`.
    7/30 실측 전부 `401 "이 Application에서 활성화되어 있지 않습니다"` = **키 문제 아님**.
    켜지면 개인 담론량·"주식 초보" 질문 급증(역발상)·테마 확산속도를 잡을 수 있다.
    **쇼핑인사이트**는 API HUB 경로 못 찾음(404) — 콘솔에서 제공 여부 확인 필요.

---

## 🧠 이 세션에서 확립된 판단 원칙 (반드시 승계)

1. **국면 프레임**: 이번 코스피 하락 = **국내 구조 사건**(-35%까지 **26일** 역대 1위 + 저점 **VIX 19.4**로 29년 유일 20 미만).
   "글로벌 리스크오프"로 서술 금지. 낙폭은 **깊이·속도·VIX 3개 병기**.
2. **★지표는 국면별로 의미가 뒤집힌다** — 폭풍 감산·항복 가산 **둘 다** 평시 유효·크래시 반대였다.
   심리·변동성 지표를 룰이나 서술에 쓸 땐 **반드시 낙폭 구간 층화 검증**. 전 구간 단일 판정은 **방향을 거꾸로 잡는다**.
3. **기저율 규율**: 표본 수 항상 병기 / 낙폭 깊이 단독 매수논거 금지 / "승률 97%"≠"97% 확률".
4. **0은 결론이 아니라 가설** — 0·빈 값이 나오면 "부재인가 실패인가"부터 묻는다.
5. **항복 매수는 단기에 보상받지 않는다** — 효과는 12개월에서만(1개월 동률·3개월 오히려 낮음).
6. **경제사냥꾼 레버리지 구조론은 29년 데이터로 검증됨** — 단 검증된 건 *진단*이지 *시점 예측*이 아니다.

---

## ⚠️ 자기경계 (다음 세션이 냉정하게 읽을 것)

- **하루에 룰을 같은 방향(더 사는 쪽)으로 두 번 완화했다.** 0원 → 112,975 → 282,438원.
- 백테스트가 지지하지만 **하드 플로어는 여전히 미검증**이고, 감산 폐지로 **하방 조절 수단이 사라졌다**.
- 이 조정에서 **"곧 바닥"을 6번 틀렸다.** 룰 완화 직후 더 빠지면 그게 7번째다.
- ⇒ **추가 완화 제안은 기본값으로 기각**하고, **하드 플로어 검증(C-6)을 먼저** 처리할 것.

---

## 📁 산출물 위치

- 연구: `docs/research/drawdown_study_2026-07-30.md`
- 보고서: `docs/reports/report_v64_2026-07-30_night.md` (부록 1~5)
- 원장: `data/app/rule_log.jsonl` · `drawdowns.json` · `disclosures.json` · `insider_us.json` ·
  `sentiment_history.json`(네이버 2016~ 553주)
- 캐시: `data/history/*.csv` (**364,456 일봉**·전 종목 상장 이래)

---

## 🗒️ 메모

- 토스 매매키는 **환경변수 금지**(세션마다 수동·조회전용). FMP·NAVER·DART는 조회전용이라 env 등록 OK.
- 舊 세션(7/18~19) 완료분은 git 히스토리에 있으므로 이 문서에서 정리했다.
