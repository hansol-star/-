# 🔧 개발 핸드오프 (다음 세션이 이어받는 메모)

> 이건 **투자 보고서가 아니라 데스크 시스템(코드·연동·룰) 작업**의 세션 간 인수인계 메모다.
> 새 세션은 이 대화 기억이 없으니 여기부터 읽고 이어간다. 완료 항목은 체크·삭제, 새 항목은 append.
> (투자 상태·시세·오더는 여전히 `docs/reports` 최신 STATE SNAPSHOT + `tasks.json`이 정본)

**최종 갱신: 2026-08-05(수) · 세션 = "시스템 전반 점검 + 외부 리서치 반영 — 도구 3종 신설"**
**git: selfcheck GATE PASS(63개 스크립트) · validate PASS**

---

## 🆕 2026-08-05 — 우리에게 없던 **세 개의 축**을 채웠다

정훈 지시: "시스템 전반 점검 + 새 기능 서치해서 반영 + 기업 자료 더 받을 것 + 주식/에이전트 리서치 참고".

### 1) 생존 점검 — 이번엔 문서와 현실이 일치했다

8/1 교훈("외부 API는 말없이 죽는다")대로 **키 붙은 레이어를 전부 실호출**했다:
DART✅ · NAVER NCP✅ · FMP(화이트리스트 NVDA)✅ · EDGAR✅ · earningscalls✅ · Yahoo✅ · FRED✅.
알려진 사망은 **KRX 공매도 하나**(데이터센터 IP 차단 — 이미 문서화됨, 12월 로컬 이전 시 자동 해소).
⇒ 문서의 "✅ 정상"이 이번엔 전부 사실이었다. **다음 세션도 이 실호출을 생략하지 말 것** —
일치했다는 결과가 아니라 *확인했다는 행위*가 이 절차의 값이다.

### 2) 외부 리서치 → 채택 3건 (우리에 **없는 것만** 골랐다)

문헌(FinMem·FinAgent·TradingAgents 계열, 상대가치 방법론)에서 우리 구조에 실제로 빠진 것만 취했다.
"좋아 보이는 것"이 아니라 **"우리 구멍에 맞는 것"** 기준.

| 신설 | 메운 구멍 | 첫 수확 (실측) |
|---|---|---|
| `edgar_search.py` | EDGAR 도구가 전부 **XBRL 태그의 숫자만** 꺼냈다 → 공시 **본문(산문)**을 못 찾았다 | `"HBM4"` = **MU 5건**(2025-10-03 10-K부터) · **NVDA·AVGO 0건** |
| `peer_compare.py` | 종목을 **세로로만**(시계열) 봤다 → 동종 대비 위치를 우리 숫자로 못 냈다 | 현대차 = 피어 **1/3위(83%ile)**인데 **자기이력 12%ile** |
| `memory_recall.py` | 원장 4종이 **서로를 몰랐다** — 쌓는 쪽만 있고 **끌어올리는 쪽이 없었다** | "MU" 한 질의에 결정 10·콜 8·열린아젠다 6·미스무브 2 통합 |

**셋 다 측정·탐색·읽기 전용 — 별점·스코어·트랜치 어떤 룰도 바꾸지 않는다.**
(8/1 `eps_revisions` 때 정훈이 세운 원칙: 검증 안 된 축은 룰에 배선하지 않는다.)

### 3) 구현 중 실측으로 잡은 것 (문서엔 안 나오는 함정)

- **EDGAR FTS는 다중 CIK를 안 받는다** — 쉼표로 여러 개 주면 **500**. 무필터 순수 질의도 500이 잦다.
  ⇒ 회사 단위로 쪼개 호출하고 합치는 구조로 갔다. 부분 실패는 전체 실패로 만들지 않는다.
- **SEC는 UA 형식에 엄격** — 연락처 없는/괄호 든 UA는 **전 요청 403**.
  `edgar_facts.py`와 **같은 문자열**을 쓰게 했다(한 곳이 막히면 전부 같이 막히도록 = 진단 단순화).
- **`financials.json`은 단위가 섞여 있다** — `*_yoy`는 퍼센트(65.47), 마진·ROE는 비율(0.6038).
  초판이 이걸 안 맞춰 **NVDA 영업마진을 "0.6%"로** 찍었다(실제 60.4%). 백분위는 단조변환이라
  순위엔 영향이 없었지만 **사람이 읽는 원값은 틀렸다** — 순위만 검토하고 넘어갔으면 못 잡았다.

### 4) R2 배선 완료 (같은 날 정훈 지시)

"몇 번 써보고 결정"이라 적어뒀으나 정훈이 **즉시 배선**을 지시 → 반영했다.

| 도구 | 배선 위치 | 호출 규율 |
|---|---|---|
| `memory_recall.py` | SKILL **§0-2c**(컨텍스트 복원, `decisions.py` 바로 뒤) | **⭐2 이하 보유 필수** + 그날 오더 종목만. **전 종목 순회 금지**(3~5회) |
| `edgar_search.py --events` | SKILL **§2 수집**(`insider_us.py` 옆 = 미국 공시 레이어) | 매 보고서. 🚨critical은 본문 노출 의무 |
| `edgar_search.py --q` | 같은 위치, **트리거 게이트** | 논지를 1차 문서로 확정할 때만. **습관적 호출 금지** |
| `peer_compare.py` | SKILL **§2 수집**(`financials.py` **바로 뒤**) | ⚠️ **순서 필수** — 먼저 돌리면 어제 숫자로 순위를 매긴다 |

플레이북에도 내렸다: **§1b-2 신설**("'동종 대비'는 이제 우리 숫자로 — sell-side 문장 받아쓰기 금지"),
risk-desk에 memory_recall·edgar_search 임무 2줄.

⚠️ **R2 트리거(claude.ai UI 생성) 프롬프트는 에이전트가 못 고친다** — `update_trigger`가
`created via http_api` 로 거부. `docs/routines.md`의 R2 블록은 갱신해뒀으니 **정훈이 UI에서
그 텍스트로 교체**하면 완결. 안 해도 트리거가 "portfolio-desk 스킬 파이프라인"을 지시하므로
SKILL.md 경유로 동작은 한다 — 다만 **무인 루틴이 프롬프트의 명시 단계를 우선하는 경향**이 있어
UI 반영을 권한다.

### 5) 배선 중 적발한 stale — 🔴 이게 이 세션에서 제일 중요하다

`desk_playbook.md` **risk-desk 고정 룰에 7/30에 폐기된 룰이 그대로 남아 있었다.**
리스크 데스크는 매 보고서 Task 0에서 이 파일을 읽는다 → **폐기된 감산 스케일을 계속 적용할 뻔했다.**

- ❌ *"안전핀 코스피 7,500"* — 매수 안전핀으로는 폐기(7/30). 7,500은 이제 **§5 해제 게이트 조건①**로만 유효.
- ❌ *"폭풍 %ile로 트랜치 금액 감산(<75%=100%…>97=25%)"* — **7/30 2차 개정으로 전면 폐기**
  (17,946표본에서 해금 4단계 전부 역효과). 폭풍은 이제 **분할 횟수만** 바꾼다(총액 불변).
- ✅ 현행 하드 플로어 = **S&P500 폭풍 ≥70%ile**.

**교훈: 룰 개정이 CLAUDE.md·crash_tf엔 반영됐는데 playbook까지 안 내려왔다.**
정본이 4개(CLAUDE.md·crash_tf·playbook·agent 파일)인데 **개정 전파를 검증하는 기계가 없다.**
→ 다음 세션 숙제: `validate_report.py`에 **폐기 문구 감지**(예: "7,500 안전핀"·구 감산 스케일이
playbook/agent 파일에 남아 있으면 WARN) 추가 검토.

### 6) 남은 것

- `peer_compare` 전력 그룹 **n=3** — 순위가 통계가 아니다. 워치 종목으로 키우려면
  `financials.py`가 보유 밖 종목을 안 받는 것부터 고쳐야 한다.
- **[8/1 미해결·이번에 재확인]** 폭풍 %ile을 `vol_gauge`(55.2)와 `garch`(75.0)가 다르게 준다
  → **하드플로어(≥70) 판정이 정반대.** 정본 미확정. playbook에 "둘 다 병기·갈리면 결정 요청" 명시해뒀다.
- `peer_compare`의 전력 그룹은 **n=3**이라 순위가 통계가 아니다. 워치 종목(두산E·GEV 등)을
  피어에 넣어 n을 키우는 안이 있으나, **보유 아닌 종목의 재무를 `financials.py`가 안 받는다** —
  확대하려면 그쪽부터.
- 남은 최대 결손은 여전히 **개별기업 CDS·글로벌 IB 리포트**(data_coverage §3 #5·#6·#10). 무료 경로 없음.

---

## 🆕 2026-08-01 — 데이터 결손을 **실측**했더니 전부 열려 있었다

정훈 지시 *"자료 더 필요한거 없나 찾아봐서 자료 보강하고, 더 주식공부해."*
`data_coverage.md §3`이 결손을 이름으로 적어두고도 **아무도 실현 가능성을 실측한 적이 없었다**
(전부 "⚠️ 조사 필요"로 이틀 방치). 4개를 실측 → **3개 해소, 1개는 불가 확정.**

| 결손 | 결과 | 산출물 |
|---|---|---|
| #3 EPS 리비전 | ✅ **완전 해소** — 33종목(국내 포함) | `eps_revisions.py` |
| #2 어닝콜/가이던스 | ⚠️ **부분 해소** — 가이던스 수치만 | `guidance.py` |
| #7b 미국 13D/13G | ✅ **해소** | `insider_us.py --major` |
| #4 DRAM 실물가 | ❌ **무료 피드 없음 확정** + FRED PPI 금지 | `data_coverage.md §3b` |

### 이 세션의 진짜 소득 = 걸러낸 **조용한 오류 6건** (전부 §4b 사고표에 등재)

`data_coverage.md §4b`가 *"결손보다 조용한 오류가 무섭다"*고 했는데, 이번에 그게 6번 재현됐다.
**여섯 번 다 "0건/부재"로 보고될 뻔했다:**

1. **13G 폼 개명** `SC 13G`→`SCHEDULE 13G` — 옛 이름 질의는 NVDA를 2024-07-18에서 끊는다(실제 2026-07-20 존재)
2. **13G 방향 혼재** — NVDA 7/20 건은 *NVIDIA가 Nebius를* 신고한 [취득]. 피취득으로 세면 정반대
3. **가이던스 파서 단위** — 표엔 마침표가 없어 문장 파서가 MU 가이던스를 통째로 버림(0건)
4. **첨부 파일명 규칙** — NVDA는 `q1fy27pr.htm`이라 `/ex99/` 필터에 안 걸려 "부재"
5. **META 앵커** — `CFO Outlook Commentary`라 못 찾음($61-64B 가이던스 존재)
6. **표본 1개 만장일치** — KT&G가 애널리스트 1명으로 "강한상향 +57.7" → `n/(n+4)` 축소로 교정

> **원칙 추가**: 새 레이어의 **폼 이름·파일명·문서 구조·방향**은 전부 조용히 바뀐다.
> "0이 나왔다"에서 멈추지 말고 **원문을 한 번 열어본다.** 이번엔 6번 다 원문에 답이 있었다.

### 투자 판단에 바로 물리는 발견

- **메모리 벨트 전면 상향**: 삼성전기 **+61.8%**·MU **+52.9%(30↑/0↓)**·삼성전자 +42.1%·하닉 +27.2%
  ⇒ **7/29 CXMT 정정("공급 타이트")을 독립 소스가 확증.** 가격 붕괴가 실재했다면 추정치가 이렇게 못 오른다
- **매수 우선순위와 리비전이 거의 역순**: 정본 GOOGL→NAVER→MU→META vs 리비전 MU≫GOOGL>META>NAVER
- **NAVER = 보유 중 유일 하향 우세(4↑/5↓)** — 희석 아닌 **마진 압박**(한화투자 7/1 "3분기까지 마진 압박 불가피",
  TP 340→300k·**3사 Buy 유지**). 당해년 -9.5%로 근거리가 더 깎임
- **MU FQ4 가이던스 GM 약 86%**(FQ3 84.9%↑) ⇒ **§10-2 기준 메모리 정점 신호 아직 아님**
- **두산로보 -76.2%** ⇒ 룰2 3/3 훼손 판정을 독립 소스가 확증

**규율**: 리비전은 **측정 전용**(정훈 8/1 지시). Yahoo가 과거 시계열을 안 줘 층화 검증 불가 →
원장 누적부터. 연구 정본 = `docs/research/eps_revision_study_2026-08-01.md`.

---

## 🔥 2026-07-31 — 룰이 하루 만에 시험대에 올랐다

코스피 **+17.91%(6,595.45) = 종가 기준 역대 최대 상승**. 어제 만든 낙폭 사다리가
만 하루 만에 실전 검증을 받았고, **판단은 옳았지만 코드에 구멍이 4개** 있었다.

| # | 결함 | 무엇이 틀렸나 | 상태 |
|---|---|---|---|
| 1 | **신선도** | 캐시(어제 종가)만 읽어 낙폭 -38.6%로 판정. 실제 -27.6%. 상한 **282,438 vs 121,045원(2.3배)** — 하필 급등·급락일에 가장 크게 틀린다 | ✅ `_stale_warning()` |
| 2 | **되돌림 정책** | 문서("첫 도달 시 해금"=래칫)와 코드(현재낙폭 재계산=RESET) **불일치** | ✅ **RESET 확정·명문화** |
| 3 | **집행 이력** | "단계 재진입 금지"가 문서에만 있고 코드엔 없었다 → 물타기 구멍 | ✅ `tranche_ledger.json` |
| 4 | **코스닥 등락률** | `chartPreviousClose`가 전전거래일 참조. **6회 재발**(사람이 매번 수기 정정) | ✅ 시계열 직접 산출 |

### 판정 근거 (`ratchet_test.py` 신설 — 11지수 24,592일)

낙폭 구간을 통제하면 **3개 구간 전부 RESET 우위**(-45%↓ ±0.0 · -45~-35% **-1.3%p** ·
-35~-25% **-0.6%p**, 구간가중 **-0.61%p**).
⚠️ **전 구간 단일집계는 +2.0%p로 부호가 뒤집힌다 — 심슨의 역설.**
7/30에 세운 원칙("지표는 국면별로 뒤집힌다")이 **하루 만에, 내가 만든 검증 도구 자신에게서** 재현됐다.

### 룰3 정량화는 **데이터가 반박해 기각**

사다리 활성 구간 당일 등락률별 12M: 하락일 +9.8% / 0~3% +9.2% / **3~6% +17.0%** / **6~10% +24.9%**.
크래시 구간에서 급등일 매수는 오히려 나았다 — 급등 금지 가드는 **폐지한 폭풍 감산과 같은 역작동**이 된다.
⇒ 오늘 집행 0원의 근거는 *"급등일이라서"가 아니라* ①+17.9%는 표본 36개 미지영역 ②게이트 1/3.

---

## ✅ 2026-07-30 세션 완료·푸시 (커밋 11개)

### 신설 스크립트 6종
| 스크립트 | 무엇 | 첫 성과 |
|---|---|---|
| `dart_disclosure.py` | 국내 수시공시(중대성 3단 분류 + elestock 내부자 합산 + majorstock 5%보고) | **NAVER 엔비디아 유상증자 적발** |
| `insider_us.py` | 미국 Form 4(재량적 매수 P와 10b5-1 매도 S 분리) | 90일 재량적 매수 **단 1건** |
| `drawdown_history.py` | 낙폭 생애주기·역대순위·조건부 기저율·매크로 오버레이 | 29년 구조 사건 판정 |
| `tranche_rules.py` | 개정 룰 1·2 판정기 | 사다리·훼손 기계화 |
| `rule_tracker.py` | 룰 자기검증 원장 + 11지수 백테스트 | 폭풍 감산 역효과 발견 |
| `capitulation_validate.py` | 항복 가산 2경로 독립 검증 | 현행 유지 확정 |

### 룰 전면 개정 (정훈 승인 "룰도 다 바꾸자" → "해금 구간 감산 제거하자")
- **룰1**: 코스피 7,500 이진 안전핀 **폐기** → **낙폭 사다리**(D1 -25%:15% / D2 -35%:20% / D3 -45%:25% / D4 -55%:25% / 예비 15%)
  - 승수 = **항복 가산만** [1.00, 1.20]. **폭풍 금액 감산 폐지**(11지수 17,946표본에서 해금 4단계 전부 역효과)
  - 폭풍은 **분할 횟수**로만(≥97 4분할 / 90~97 3분할 / <90 2분할, 총액 불변)
  - **하드 플로어 = S&P500 폭풍 ≥70%ile(글로벌 확산)** → 사다리 전면 정지
  - **오늘 상한: 0원 → 112,975 → 282,438원**
- **룰2**: LG전자 전용·이벤트형 → **전 종목·추세형 3중**(마진 3년↓·FCF 악화·순부채↑)
  - 3/3 훼손 착수 / 2/3 감시상향 / ≤1 정상. **금융 자회사 예외**(현대차 = 마진 조건만)
  - 첫 판정: **두산로보 3/3 훼손 착수** · 현대차 마진 훼손(1/1) · 나머지 정상

### 정본 문서 전면 개정
`CLAUDE.md` · `crash_tf.md`(§0 프레임·§2b 사다리·§5b 폐기·§5c 역사좌표·§8 추세형) · `master.md`(§3 룰·§9 결정 6건) ·
`holdings_outlook.md`(한 달 stale → 전면 재작성) · `desk_playbook.md`(§2 규율 4건) · `data_coverage.md`(§4b 사고표·§5b 요청) ·
`self-review SKILL`(§7 역량감사·§8 룰추적) · `routines.md`(R3 배선) · 데스크 4종(semi/bigtech/power/risk/us)

### 교정한 사고 6건
- **두산로보 티커 `.KQ` 오염** — 재무제표에 코스닥 남의 회사. "커버리지 14/14"가 빈 껍데기였다
- **Form 4 XSL 경로** — HTML 파싱으로 전 종목 "0건"
- **수집 실패를 "0건"으로 보고** — 실패와 부재를 구분하도록 수정
- **지수 파일명 규칙 불일치** — 코스피·나스닥·S&P가 통째로 "캐시 없음"
- **`score()`가 전방수익률을 항상 코스피로 계산** — 풀링 후 그대로였으면 표 전체가 무의미
- **NAVER 유상증자 대상 미확인 방치** — 검색 한 번이면 풀렸다(정훈 지적)

---

## 🔓 열린 작업 (우선순위 순)

### A. 즉시 (8/3 월 · 주말엔 8/1~2 베이킹)

1. **외인 3일째 지속 여부** — 게이트②가 7/30 +1.34조 → 7/31 **+7.24조**로 첫 점등했다(1/3).
   진짜 전환인지 **숏커버인지** 대차잔고·공매도잔고로 분리 판별. 반납되면 **V반등 7번째 실패**.
2. ~~`financials.json` 갱신 미완~~ → ✅ **7/31 완료**(백그라운드 완주, 14종목 updated=7/31).
   갱신 후 룰2 재판정도 **결과 동일**: 두산로보 3/3 훼손 착수 · 현대차 마진 1/1(금융연결) · LG전자 1/3 정상.
3. **GOOGL 1차 체결 확인**(룰8 — 체결 전 완료 처리 금지). 매수 1순위 유지.
4. **8/7 NFP·8/12 CPI·8/13 PPI = 전부 21:30 = 폰창 밖** → 주말에 조건부 지정가 사전 베이킹.

### B. 딥다이브 부채 (룰2 판정에서 발생)
4. **현대차 제조 수익성 딥다이브** — 영업마진 9.3→8.1→6.2%. **금융부문 제외 제조 마진 분리**가 핵심.
5. **두산로보틱스 3/3 훼손 착수** — 이미 ⭐2 모멘텀이라 코어 아님. 트림 검토 여부만 판단.

### C. 검증 부채 (rule_tracker가 추적)
6. **🔴 하드 플로어(S&P ≥70%ile) 미검증** — 발동 표본 0건, 백테스트에서도 미적용.
   **감산 폐지로 유일하게 남은 하방 장치**라 최우선. 검증 방법 설계 필요.
7. **실시간 원장 표본 1개** (40 필요) — 매 보고서·R3에서 `rule_tracker.py --snapshot` 누적.
8. **룰2 훼손 판정 종목 후행 추적** — 두산로보·현대차 이후 주가로 경보 적중 채점(`--score` ⑤).
8b. **`debt_buildup` 플래그가 순현금만 본다** — ORCL 총부채 $132B→**$219B(1년 +48%)**·FCF -$23.7B·
    **S&P BBB- 하향**인데 플래그가 안 떴다. 부채로 조달한 현금을 쌓아두면 레버리지가 가려진다.
    **총부채 증가율 조건 추가 필요.**
8c. **심리 데이터 구조적 1일 지연** — 네이버 트렌드가 D-1까지만 제공. 항복 가산(×1.2)은 항상
    하루 늦은 심리로 계산된다. 7/31 재측정해도 7/30 기준값(공포 97.4·항복 98.6)이 나왔다.
8d. **`vol_sizing.py`가 폐기된 舊 "코스피 7,500 안전핀" 문구를 그대로 출력** — 사다리 개정 미반영.
8e. **STM 재무 커버리지 0/1** — EDGAR 20-F(외국민간발행인) 구조 미대응.

### D. 남은 데이터 결손 (`data_coverage.md` §3) — 8/1 갱신
9. ~~EPS 리비전(#3)~~ ✅ **해소** · ~~13D/13G(#7b)~~ ✅ **해소** · ~~DRAM 실물가(#4)~~ ❌ **불가 확정**(FRED PPI 금지 명문화)
10. **어닝콜 Q&A 전문(#2 잔여분)** — 가이던스 수치는 `guidance.py`로 잡았으나 **AAPL·MSFT·GOOGL·ORCL은
    보도자료에 수치를 안 낸다**(구두만). **매수 1순위 GOOGL이 여기 속해** 우선순위 높음. 무료 소스 재조사
11. 개별기업 CDS(#5) · 글로벌 IB 리포트(#6) — 둘 다 유료 벽

### D1c. 🔴 8/1 3차 — **ORCL 총차입금이 18배 과소집계되고 있었다** (최중요)

`§C-8b`가 *"debt_buildup이 순현금만 본다"*로 적혀 있었는데, 파보니 **플래그가 아니라 그 아래
데이터가 틀렸다.** ORCL은 `LongTermDebtNoncurrent`를 안 쓰고
`DebtLongtermAndShorttermCombinedAmount`로 신고하는데 그 태그가 목록에 없어 **`DebtCurrent`
(단기 $7.2B)만** 잡혔다.

| | 수정 전 | 수정 후 |
|---|---|---|
| ORCL 총차입금 | $7.2B | **$129.5B** (18배) |
| ORCL 순현금 | **+$24.1B (흑자)** | **-$98.3B (순부채)** ← **부호 반전** |
| ORCL 룰2 | 순부채 조건 **발동 불가** | **2/3 ⚠️ 감시 등급 상향** |

**조치**: `edgar_facts.py`에 `DEBT_COMBINED` 신설(combined 우선·이중계상 방지) +
`validate_report.check_debt_integrity()` **새니티 가드 신설**(total_debt이 총부채의 5% 미만이거나
결측이면 WARN). ANET은 **EDGAR 부채 태그가 2013년 이후 없음 = 실제 무차입** 확인 후
`DEBT_FREE_VERIFIED` 등재. AVGO도 $65.1B→**$67.1B** 개선(combined 단독값·이중계상 아님).
나머지 12종목 무변동(회귀 없음). 음성테스트로 가드 작동 확인.

**`debt_buildup` 보강**: 순현금 악화 **또는 총차입금 YoY +30%**. 순현금 맥락을 메시지에 병기해
*캐펙스 조달*과 *레버리지 누적*을 구분한다(경보 피로 방지).

**🔴 이 수정으로 드러난 투자 함의 — GOOGL·META가 순부채로 돌아섰다**
- **GOOGL 총차입금 $11.9B → $48.5B (YoY +308.5%)**, 순현금 +$11.6B → **순부채 -$17.8B** [high]
  ⇒ 매수 1순위인데, 같은 날 트랜스크립트에서 확인한 **캐펙스 가이던스 $195-205B**와 정확히 맞물린다.
  ⇒ **리비전이 미지근한 이유(+4.9%)가 이걸로 설명된다** — 이익이 아니라 투자로 가고 있다.
- **META 총차입금 $28.8B → $58.7B (+103.8%)**, 순부채 -$22.9B [high]
- 삼성전자 +30.6%이나 **순현금 +32.6조 여유 = 캐펙스 조달 성격**(위기 아님)
- ⚠️ 단 **룰2 판정은 GOOGL 0/3·META 1/3으로 여전히 정상** — 부채만으로 훼손이 아니다.

### D1b. 8/1 후속 — 정훈 "못하면 부탁해라" → 실측했더니 2건이 부탁 없이 풀렸다
- ✅ **어닝콜 Q&A 전문(#2 잔여분) 완전 해소** — `transcripts.py`(earningscalls.dev **무키·무가입**).
  9/9 종목 3.6만~6.7만자. **GOOGL 사각지대 해소**가 핵심(Cloud +82%·백로그 $514B·캐펙스 $195-205B).
- 🔴 **FMP 사망 발견** — v3 전면 차단(NVDA조차). stable은 화이트리스트 유지. **유료 전환 금지**(§5c).
  `fundamentals.py`는 현재 **작동 불가 상태** — 대체재(EDGAR·Yahoo·DART)가 이미 1차라 우선순위 낮음.
  ⚠️ 다만 **문서가 "✅ 정상"이라 거짓말하고 있었다** — 키 붙은 레이어 주기적 실호출 점검을 §4에 편입할 것.
- 🙏 **정훈 요청 목록 정본 = `data_coverage.md §5b-2`** (1.네이버 토글 0원 2.ECOS 키 0원 3.증권사 리포트 0원)

### D2. 8/1 세션이 남긴 숙제
12. **NAVER 마진 압박 지속기간** — 한화투자 "3분기까지". `broker_reports.py --fetch`로 **PDF 본문** 대조
    (현재 GPU 7,000억→1조·마진 18→17.3% 수치는 **2차 출처라 [부분확인]**)
13. **메모리 리비전의 시차 검증** — 7/28~29 폭락이 30일 창에 덜 반영됐을 수 있다.
    **다음 `eps_revisions.py --save`에서 하향 전환 여부 확인**(원장 2번째 표본이 답한다)
14. **삼성전기(+61.8%, 유니버스 1위)** — 워치에만 두고 있다. MLCC·기판 AI 수혜 딥다이브 후보
15. **매수 우선순위 재검토** — 낙폭 기저율(7/30)과 리비전(8/1)이 MU·NAVER에서 정반대.
    **다음 보고서 PM 사견에서 명시적으로 다룰 것**(둘은 서로 다른 질문에 답한다 — 어느 쪽에 무게를?)

### E. 정훈 요청 대기 (막힌 게 아니라 콘솔 토글)
13. **네이버 API 4종 활성화** — `cafearticle`·`kin`·`blog`·`webkr`.
    7/30 실측 전부 `401 "이 Application에서 활성화되어 있지 않습니다"` = **키 문제 아님**.
    켜지면 개인 담론량·"주식 초보" 질문 급증(역발상)·테마 확산속도를 잡을 수 있다.
    **쇼핑인사이트**는 API HUB 경로 못 찾음(404) — 콘솔에서 제공 여부 확인 필요.

---

## 🧠 이 세션에서 확립된 판단 원칙 (반드시 승계)

1. **국면 프레임**: 이번 코스피 하락 = **국내 구조 사건**(-35%까지 **26일** 역대 1위 + 저점 **VIX 19.4**로 29년 유일 20 미만).
   "글로벌 리스크오프"로 서술 금지. 낙폭은 **깊이·속도·VIX 3개 병기**.
2. **★지표는 국면별로 의미가 뒤집힌다** — 폭풍 감산·항복 가산 **둘 다** 평시 유효·크래시 반대였다.
   심리·변동성 지표를 룰이나 서술에 쓸 땐 **반드시 낙폭 구간 층화 검증**. 전 구간 단일 판정은 **방향을 거꾸로 잡는다**.
3. **기저율 규율**: 표본 수 항상 병기 / 낙폭 깊이 단독 매수논거 금지 / "승률 97%"≠"97% 확률".
4. **0은 결론이 아니라 가설** — 0·빈 값이 나오면 "부재인가 실패인가"부터 묻는다.
5. **항복 매수는 단기에 보상받지 않는다** — 효과는 12개월에서만(1개월 동률·3개월 오히려 낮음).
6. **경제사냥꾼 레버리지 구조론은 29년 데이터로 검증됨** — 단 검증된 건 *진단*이지 *시점 예측*이 아니다.

---

## ⚠️ 자기경계 (다음 세션이 냉정하게 읽을 것)

- 7/30에 *"룰 완화 직후 더 빠지면 7번째 오판"*이라 적었는데 **반대로 갔다**(역대 최대 상승).
  ⇒ **그렇다고 어제의 신중이 틀렸던 게 아니다.** 하루 결과로 판단 *과정*을 채점하면
  다음엔 결과에 끌려다니게 된다. 지금 경계 대상은 **"드디어 바닥이었다"는 낙관 편향**이고,
  이건 어제의 비관 편향과 **같은 실수의 다른 얼굴**이다.
- **집계 지표가 두 번 연속 방향을 거꾸로 잡았다** — ①분모 0인 대상과 비교(-25% 위 구간)
  ②심슨의 역설(전 구간 단일집계). **검증 도구를 만들 때도 국면 층화부터** 할 것.
- 하드 플로어는 **여전히 미검증**이고, 리스크 데스크 지적대로 **국내 단일일 스퀴즈엔 애초에
  반응하지 않는** 장치다. 감산 폐지 후 남은 하방 장치가 오늘의 실제 위험엔 침묵했다.
- **추가 완화 제안은 여전히 기본값 기각.** 게이트는 1/3이지 2/3가 아니다.

## 📁 산출물 위치

- 연구: `docs/research/drawdown_study_2026-07-30.md`
- 보고서: `docs/reports/report_v64_2026-07-30_night.md` (부록 1~5)
- 원장: `data/app/rule_log.jsonl` · `drawdowns.json` · `disclosures.json` · `insider_us.json` ·
  `sentiment_history.json`(네이버 2016~ 553주)
- 캐시: `data/history/*.csv` (**364,456 일봉**·전 종목 상장 이래)

---

## 🗒️ 메모

- 토스 매매키는 **환경변수 금지**(세션마다 수동·조회전용). FMP·NAVER·DART는 조회전용이라 env 등록 OK.
- 舊 세션(7/18~19) 완료분은 git 히스토리에 있으므로 이 문서에서 정리했다.
