# 영구 교정 사항 — 전문 (재발 금지)

> **정본이다.** 2026-09-09 CLAUDE.md 다이어트로 이 문서로 옮겼다 — 내용은 **한 글자도 안 지웠고**
> 위치만 바뀌었다. CLAUDE.md엔 요약 표만 남고, 상세는 여기가 정본이다.
>
> **왜 옮겼나**: CLAUDE.md는 **모든 세션·모든 서브에이전트에 자동 로드**된다.
> 실측(9/9) — 데스크 하나가 평균 **13만 토큰**인데 실제 작업은 3만 안팎이고,
> 나머지가 문서 로드였다. 이 섹션만 17,683자였다.
> 그리고 8/27 오류 감사가 이미 결론을 냈다: **교훈을 산문으로 적는 것은 재발을 막지 못한다**
> (150건 중 기계화된 건 26%뿐). 안 지켜지면서 매 세션 토큰만 먹는 문장은 여기 두고,
> **가드가 있는 것만 CLAUDE.md에 한 줄로 남긴다.**
>
> ⚠️ **읽어야 할 때**: 같은 클래스의 실수를 의심할 때 · 룰/도구를 고칠 때 · self-review(R3).
> ⚠️ 가드가 없는 항목(아래 🔴 표시)은 **여전히 사람이 기억해야 하는 것**이다.

- **★[8/31 신설 — 환경을 옮기면 '맞던 명령'이 조용히 틀리기 시작한다. 열린 것보다 이걸 찾는 게 어려웠다]** 로컬 이전 첫날, 기대했던 것(yt-dlp)은 예상대로 열렸는데 **아무도 안 건드린 코드 3곳이 그날부터 틀리기 시작했다.** 셋 다 예외를 안 던지고 **정상처럼 보이는 값**을 냈다. ①**`TZ=Asia/Seoul date`가 UTC를 준다** — MSYS(Git Bash)엔 `/usr/share/zoneinfo`가 없어 알 수 없는 TZ 이름이 UTC로 폴백한다(`date` 16:50 vs `TZ=Asia/Seoul date` **07:50**). 지시층 6곳이 이 명령을 쓰고 있었고, **00~09시 KST엔 어제가 나온다** = 7/6 날짜 앵커링 사고의 재발 경로. ②**그 틀린 시각을 `local_doctor`가 ✅로 찍고 있었다** — 윈도우엔 `time.tzset()`이 없어 except로 넘어가고 MSVC가 "Asia/Seoul"을 못 읽어 UTC로 떨어진 07:49를 KST라며 통과(`%Z`는 `a/S` 쓰레기). **날짜를 지키라고 만든 가드가 스스로 틀린 날짜를 만들었다.** ③**설치된 yt-dlp가 '미설치'로 판정됐다** — 윈도우 pip은 `Scripts/`에 깔고 그 디렉터리는 PATH에 없을 수 있는데 call site 5곳이 전부 `shutil.which("yt-dlp")` → None → `return None`으로 조용히 폴백. **이전의 최대 성과가 발견 실패 한 줄에 통째로 묻힐 뻔했다.**
  **무엇이 잡는가**(8/27 규율): ①②는 `kst_now.py`(UTC+9 고정·OS 무관) + `local_doctor`의 KST 항목이 **시스템 시계와의 skew**를 재도록 교체 ③은 `ytdlp_bin.py`(PATH → `-m yt_dlp` 해석)와 `api_health`의 yt-dlp 판정을 **메타 통과가 아니라 스트림 개수**로 교체(8/22 "메타 성공 ≠ 생존"의 기계화).
  **파생 규율**: 환경(OS·IP·런타임)이 바뀌면 **"실행파일 이름·TZ 이름·경로 규약"에 기댄 코드를 먼저 의심한다.** 이 셋은 예외를 안 던지고 **그럴듯한 값**을 돌려주는 종류라 테스트가 아니라 **다른 경로로 잰 값과의 대조**로만 잡힌다(훅 앵커 16:49 ↔ doctor 07:49의 불일치가 실제 단서였다). ⚠️ 같은 세션에서 `local_doctor`가 **폐기된 토스 키 룰**(저장 금지 → 8/31 저장 허용으로 개정)을 아직 집행하며 승인된 설정을 FAIL로 찍고 있었다 — 8/23 `check_repealed_rules`와 같은 클래스. 검사 대상을 "키가 없는가"에서 **"주문 차단 가드가 실제로 막는가"**(POST /orders 등 4종 차단 + GET 통과 실증)로 교체했다.

- **★[8/27 신설 — 교훈을 산문으로 적는 것은 재발을 막지 못한다. 세어봤다]** 정훈 *"계속 틀리고 수정하고 정보 안찾아보고 미확정이고 이런 상황들이 왜 일어나는거야"* 에 답하려고 영구교정 **150건**(CLAUDE.md 34 + master §7 116)을 오류 클래스로 분류해 가드 유무와 교차했다(정본 = `docs/research/error_pattern_audit_2026-08-27.md`). 결과가 갈렸다: **가드가 붙은 클래스는 재발이 멈췄고**(D stale 6건→**0** · H 지표설계 5건→**0** · G 물리제약 5건→1), **가드가 없는 클래스만 4개월째 그대로 재발했다**(C 단위·자릿수 16건 5/7~8/23 상시 · A 확인 전 단정 6건 → **8/27 금통위로 또 재발** · B 1차출처 2건). CLAUDE.md 영구교정 34건 중 **기계화된 건 9건(26%)**뿐이었다. ⇒ **조치**: 무가드 3클래스에 가드 신설(`check_verdict_grounding`·`check_magnitude_sanity`·`check_primary_source`) + 주입 테스트 등록(커버리지 6/30 → **9/33**). ⚠️ **D·E의 '가드 후 0건'은 가드가 만들어진 지 4~5일이라 표본이 짧다** — 방향은 일관되나 확정으로 읽지 말 것. ⚠️ **커버리지 9/33은 성취가 아니라 결핍 목록**이다.
  **파생 규율 3개**: ①**교훈을 적을 때 '이걸 무엇이 잡는가'를 같이 적는다.** 잡을 장치가 없으면 그건 교훈이 아니라 소망이다. ②**[대립·미결] 태그 신설** — hunter 아카이브 500건에서 검증 367·정정 48·미확인 85인데 **대립·미결은 0**이었다(카테고리가 산문에만 있고 어휘에 없었다). *결과가 아직 안 나온 대립*이 [정정]으로 밀리면 **컨센서스를 사실로 취급**하게 된다(8/27 금통위 = 채널이 맞았다). **[정정]은 사실 확정 후에만.** ③**'미확인'을 셋으로 가른다** — ⓐ확인 불가(유료·라이브) ⓑ확인 안 함 ⓒ아직 결과 없음. 한 바구니면 ⓒ가 ⓑ처럼 보여 아무도 판별 못 한다(8/12 '분모를 의심하라'의 연장).
  ⚠️ **가드 제작 중 그 가드가 두 번 틀렸다**: ±320자 윈도우가 옆 섹션까지 넘어가 거기 '체결'을 결과어 알리바이로 주웠고(→금통위 사례 통과), 제목줄 *"확인 전 단정"*의 '확인'도 알리바이가 됐다. **둘 다 주입 테스트가 아니었으면 '돌지만 아무것도 못 잡는' 가드로 커밋됐다** — 8/22·8/23 교훈의 세 번째 실증.
  ⚠️ **남은 근본 원인은 예산이다(미해결·d152)**: 8/27 무인 루틴 3개가 전부 토큰 한도로 죽었고 R2는 18분 만에 잘렸다. **완주 압박은 곧 교차검증 생략 압박**이라, "정보 안 찾아봤다"의 상당 부분은 의지가 아니라 이 압력이다. 가드를 늘려도 확인할 시간이 없으면 같은 일이 난다.
- **★[8/24 정훈 지적 — "계속 너 달러가 있다는 걸 까먹고 말하는 거 같다"] 현금은 한 덩어리가 아니라 2계정이다.** 정훈 현금 = **원화 266,060원 + 달러 $308.94**(합산 693,367원). 이걸 합산액으로만 말하면 두 종류의 오류가 난다: ①**미국주 매수에 "환율 진입 타이밍" 논거를 붙이는 오류** — 토스는 **보유 USD를 먼저 소진**하고 부족분만 환전하므로(8/14 VOO 건에서 실측), 달러 잔고 안에서 사는 매수엔 **환전 자체가 없다.** GOOGL $25 건에서 "원/달러 1,383원 = 1년 최저권이라 타이밍 좋다"고 쓴 건 무효 논거였고, 오히려 원화 강세는 **보유 달러의 원화 환산가치를 깎는 역풍**이다(`fx_exposure` 환 기여 -323,800원이 이미 그걸 재고 있다). ②**"재원 부족"과 "룰 상한"을 뭉뚱그리는 오류** — 삼성전자 1주(257,000원)는 원화 잔고 266,060원으로 **살 수 있다**. 막는 건 현금이 아니라 **사다리 상한 104,004원**이다. MU 3차도 같다(달러는 있는데 상한이 모자란다). **규칙**: 재원을 말할 땐 ⓐ**결제 통화**(미국주=USD·국내주=KRW) ⓑ**해당 통화 잔고** ⓒ**룰 상한**을 분리해서 쓰고, "못 산다"고 할 땐 **셋 중 무엇이 막는지 명시**한다. ⚠️환전이 실제로 일어나는 건 **달러 잔고를 초과하는 매수**일 때뿐이며, 그때만 환율 타이밍이 논거가 된다.
- **★[7/6 정훈 지적 — 날짜 앵커링 오류로 보고서 하루 밀림·재발 절대금지]**: 세션 시작 시 시스템 currentDate가 **2026-07-06**임에도, 직전 v39(7/4 토 주말 보고서)의 "주말 휴장·다음 세션=7/6" 서사에 끌려 **오늘을 7/5(일)로 착각** → 7/6(월) 새 거래일에 v40을 냈어야 하는데 v39에 "7/5 부록"을 덧붙임. **근본원인 2가지**: ①**실제 날짜·요일 미확인** — `date`(KST)로 오늘을 실측하지 않고 보고서 내부 서사에서 추론(하루 밀림). ②**실시세 미조회로 장 개장 신호 놓침** — "주말 휴장" 단정하고 `market_data.py`를 초반에 안 돌려, 코스피가 실제로는 **8,177 장중 등락**(=평일 개장) 중인 걸 못 봄. `pnl.py`(프로즌 종가 재사용)만 돌림. **보완 룰(스킬 §0 반영)**: 세션 시작 시 **①먼저 `kst_now.py`로 실제 날짜·요일·시각(★8/31: `TZ=Asia/Seoul date`는 윈도우에서 UTC로 9시간 밀린다 — 금지) ②`market_data.py`로 지수 실시세 → 코스피가 '장중 등락'이면 평일 개장·'종가 고정'이면 휴장**을 교차검증. **시스템 currentDate·실시세가 날짜의 정본, 직전 보고서의 '오늘/다음 세션' 표기는 참조용.** 새 거래일이면 반드시 **새 번호 보고서(v+1)** — 직전 보고서에 부록으로 덧붙이지 말 것. **[7/14 기계화]**: SessionStart 훅(`.claude/hooks/session-start.sh`)이 모든 세션(루틴 포함) 시작 시 KST 날짜·요일·장세션 상태·최신 보고서 버전·TF 상태를 자동 주입 — 훅 앵커가 날짜의 1차 정본, 실시세 교차검증(②)은 여전히 수행.
- **★[8/2 정훈 지시 "왜 따라가지 못했는지 분석하고 발전시키자" — 집행 배선 가드 신설]**: 함께한 7주(6/13~8/1) 성과 -7.2%의 손실 68.7%가 ⭐2 두 종목(현대차·두산로보)에서 났는데, **보고서 산문엔 트림이 8~9회 등장했으나 `tasks.json` orders엔 0회 등록**이었다(`git log -S` 실측). 체결된 7건은 **전부 orders 경유**. ⇒ **오더북에 들어간 것만 집행된다 — 산문에 남은 판단은 증발한다.** **영구 원칙: '관망'은 결정이 아니다.** ⭐2 이하 보유는 매 보고서에서 ①트림 오더(가격·수량) 또는 ②'홀드 + 재검토 트리거·기한' 중 하나를 반드시 낸다. 기계 강제 = `validate_report.py` 3종(`check_low_star_action` FAIL · `check_prose_order_link` WARN · `check_pending_decisions` 7d WARN/14d FAIL). ⚠️ 부수 확정: **크래시 TF는 '매수' 동결이지 매도 동결이 아니다** — 트림은 TF와 무관하게 집행 가능(동결이 리스크 축소까지 얼린 것이 방치의 공범).
- **[7/12 정훈 지적 — tasks.json 내용 stale로 앱 #plan 화면 옛날 정보·재발방지 가드 신설]**: v46 R2가 stocks·hunter·flows는 갱신하고 **tasks.json은 `as_of`/`source_report`만 v46로 바꾸고 outlook·index_forecast·오늘할일 내용은 v45(7/9) 상태로 방치** → 앱 #plan 화면이 "오늘 7/9 미장 개장"·코스피 ref 7,247 등 옛 정보 노출. validate가 못 잡은 구멍 = `check_tasks()`가 source_report 존재만 보고 내용 신선도 미검사(source_report는 정확했으니 통과). **근본수정(validate_report.py `check_freshness`에 2겹 가드 신설)**: ①**tasks.json `updated` < 최신 보고서 날짜 → FAIL**(§3b 매 보고서 동기화 의무 = updated는 보고서 날짜와 같아야) ②**index_forecast 코스피 ref vs flows 최신 종가 >3% 이격 → WARN**(updated만 갱신하고 숫자 안 고친 내용-stale 감지). 음성테스트로 원버그(updated 7/9·ref 7247) FAIL+WARN 재현 확인. **교훈**: R2/부록 세션은 tasks.json도 stocks처럼 내용 전체(outlook·index_forecast·today/week/month 할일)를 그날 상태로 새로 채운다 — 스탬프만 바꾸지 말 것.
- **[7/7 정정·확정] 한국은행은 2026년 2월부터 자체 점도표를 발표한다** — 舊 "한은은 점도표 미발표(연준 전용)"는 폐기. 연준 점도표와 별개 제도로, 금통위원 7인이 **향후 6개월 금리전망**을 제시(3개월 포워드가이던스에 추가하는 게 아니라 6개월 이후 전망치, 위원당 3점=익명성·확률분포 목적), **경제전망 발표월(2·5·8·11월) 공개**. 2026.2월 첫 점도표 평균 2.46%·편차 0.12%. 6개+ 독립매체 교차검증(정훈 7/7 확인 위임). ※연준 점도표(FOMC 전용)와 혼동 말 것 — 둘 다 존재.
- **[7/26 정정 — 계약 금액은 주체·범위·형식을 함께 박는다]** v60 부록이 AI서밋 건을 "SK하이닉스-엔비디아 **$7,500억** 5년 HBM"으로 적었으나 **주체·범위 오기**. 1차 출처(NVIDIA 뉴스룸·SK하이닉스 뉴스룸·CNBC) 확정: **SK그룹-엔비디아 = $5,000억+ · 형식은 LOI**(SKT 2GW AI팩토리[DSX·베라루빈, 1호 2027] + SK하이닉스 차세대 AI메모리 공동개발), **$7,500억(≈1,100조)은 SK그룹-빅테크 3사 5년 총액**(엔비디아 $5,000억 + MS·앤트로픽 등 $2,500억). **재발방지 규칙**: 계약·MOU 금액을 쓸 때 ①주체(그룹 vs 계열사) ②상대(단독 vs 복수) ③형식(LOI/MOU vs 구속력 있는 확정계약)을 **항상 같이 표기**한다. 큰 숫자일수록 매체가 합계·단독을 섞으므로 **회사 공식 뉴스룸(1차 출처)까지 내려가 확인**.
- **[7/28 정정 — 금리·스프레드 지표는 단위(bp/%p)와 절대수준을 항상 병기]** v62 본편이 엔비디아 CDS를 "**4~5%p 상승**"으로 적었으나 실제는 **+0.14%p(14bp) 상승 → 절대수준 0.82%(82bp)**(5년물, 2025.11 거래개시 이래 최대 일간 상승). 방향·의미는 맞았으나 **크기가 30배 과장** — %(변화율)과 %p(절대폭) 혼동으로 추정. **재발방지 규칙**: CDS·스프레드·금리 변동을 쓸 때 ①**단위를 bp 또는 %p로 명시** ②**변동폭과 절대수준을 같이 표기**(예 "+14bp → 82bp") ③**자릿수 sanity check**(투자등급 CDS가 하루에 4~5%p 움직이면 정크 붕괴급 — 상식적으로 불가능한 수치를 그대로 쓰지 말 것). 같은 세션에서 엔비디아-OpenAI 딜도 **$2,500억(오하이오 10GW DC 리스료 보증)+$3,500억(GPU 구매 파이낸싱, 검토중)**로 확인 — Bloomberg 총칭 "$7,500억 딜 푸시"는 **SK그룹-빅테크 5년 $7,500억과 전혀 다른 건**이므로 7/26 정정룰(주체·범위·형식 병기)을 큰 숫자마다 재적용할 것.
- **★[7/29 정정 — CXMT는 "싼 대안"이 아니다. 삼성보다 비싸게 판다 (정훈 지적으로 발견)]** 우리 문서(crash_tf ⑪ 등)가 "범용 DDR 가격 압박은 실재(**DDR4 반값 공급**)"로 적어둔 건 **틀렸다**. 실제: **CXMT 64GB 서버 DDR5 모듈이 삼성(~$1,240/개)보다 비싸고**, **화웨이의 할인 요구도 거절**했으며, **애플 협상에서도 삼성보다 높은 가격을 제시**했다(Tom's Hardware·wccftech·TechPowerUp·Notebookcheck·테크월드 다출처 [검증]). **CXMT의 경쟁우위는 가격이 아니라 물량(supply)이다.** 점유율은 **7%(세계 4위·매출 기준)** — ★[8/30 재정정] 이 자리에 적었던 *'~11%'*는 **틀렸다**. Counterpoint Q2 2026 Global Memory Tracker 실측 = 삼성 39%·SK하이닉스 26%·마이크론 25%·**CXMT 7%(+716% YoY)**이고, CXMT는 1Q 8%→2Q **7%로 오히려 하락**했다(삼성은 38%→39% 상승). 7/29에 CXMT를 정정하면서 **같은 문장 안에서 점유율을 또 틀린 것** — 2차 매체 수치를 1차 집계기관 확인 없이 옮긴 게 원인이고, 경제사냥꾼 8/29 영상이 이 오류를 잡아냈다. ⚠️**신규 규율: 점유율은 매출(revenue) 기준인지 비트(물량) 기준인지 항상 병기**한다 — 기준이 다르면 숫자가 갈린다. **⇒ 함의 반전**: ⓐ신규 진입자조차 프리미엄을 받는다 = **공급 타이트의 실물 증거 → 메모리 롱(삼성·MU·하닉)에 강세 재료**이자 7/28~29 폭락 서사("중국이 물량 쏟아부어 가격 붕괴")의 **정면 반증** ⓑ애플의 CXMT 접근은 원가 절감이 아니라 **물량 확보 + 협상 카드**(BofA) → **애플은 메모리 원가 탈출구가 없다 = AAPL 마진 리스크는 종전 평가보다 크다.** **재발방지**: 신규 진입자·후발주자를 자동으로 "저가 공세"로 전제하지 말 것 — **실제 호가(계약가)를 1차 출처로 확인**한 뒤 서술한다. 공급부족 국면에서는 후발주자도 프리미엄을 받는다.
- **[7/29 정정 — F4 레버리지ETF 규제 시행일: 8/5·8/19 → 7/31 동시 조기 시행]** 우리 문서 전반(master·crash_tf §8b-4·portfolio.json EVENT·tasks.json·v54~v63 보고서)에 "8/5 기본예탁금·8/19 현금전용"으로 박혀 있던 시행일은 **틀렸다**. 금융위·금감원·거래소가 **7/24 발표로 둘 다 7/31 동시 조기 시행**으로 앞당겼다(한경·한국일보·fnnews 교차 확인). 내용 = 기본예탁금 1,000만→**3,000만원 현금**·대용증권 **현금전용**·매도대금 인정 **매도일+2영업일**·완화 불가. **함의**: 금융위 추산 7~8조 청산압력의 **선제청산 창이 8월 초까지가 아니라 7/30~31로 압축** → 타이밍 판단(반등 시점·재진입 순서)이 통째로 바뀐다. **재발방지**: 규제·시행일은 발표 시점 값을 그대로 굳히지 말고 **시행 D-7 이내에 재확인**할 것 — 당국은 시장 상황에 따라 조기·유예 변경을 자주 한다.
- **[7/29 프레임 — 낙폭은 반드시 '고점대비'와 'YTD'를 병기한다]** 코스피 7/29 종가 5,663.24 = 고점(6/22 **9,114.55**) 대비 **-37.9%** · 7월 월간 **-33.2%**(7/28 -28.9%에서 확대)지만, 연초(1/2 4,309.63) 대비로는 **여전히 +31.4%**다. 상반기에 **+111.5%**(시총 3,558.7조→6,929.5조) 오른 지수의 되감기라는 사실이 낙폭 숫자만 쓰면 사라진다. **어느 한쪽만 쓰면 방향을 잃으므로 항상 병기.**
- **[7/28 확정 — 2026년 7월 코스피 월간 -28.9% = 사상 최악 월간 낙폭]** 2008.10 금융위기 **-23.1%**·1987.10 블랙먼데이 **-27.2%**·2000.11 IT버블 **-22.9%**를 전부 초과. 6/22 고점 **9,114.55** → 7/28 종가 **6,023.66 = 고점 대비 -33.9%**(코스닥 고점대비 -53%). 12M fwd PER **5.1배**(반도체 4배 이하; 타 집계 6~7배 = 금융위기 이후 최저권). ⇒ **이번 국면을 "평범한 조정"으로 서술하지 말 것** — 원인 진단(구조·심리쇼크)과 낙폭 크기(역사적 극단)는 별개로 기술한다.
- 코스피 6/12 종가 **8,123.62(+4.63%)** 확정 — 외인 25거래일 만 순매수 전환.
- SPCX 상장 첫날(6/12) 종가 **$160.95(+19.2%)** 확정.
- 샘 올트먼 방한 **연기**(6/15 예정이었음, 새 일정 미정).
- LG전자 기준 원가 155,200원 스크린샷 검증 완료.
- 경제사냥꾼 정정 이력: 외인 주도 회복(실제 기관 주도), 삼성 파업 진행 중(실제 타결).
- **[6/20] 디스클레이머 거슬림**: "투자 자문 아님"을 채팅·표마다 반복하지 말 것 → 보고서 파일 끝 1회만.
- **[6/20] 보유 풀표 채팅 누락 2회 위반**: 보유 전종목 + 워치 풀표(별점·매수존·매도/트림 컬럼 포함)를 채팅 응답에 항상 출력. 후속 메시지(이슈 심층·머지)에서도 생략 금지.
- **[6/14 해소·7/1 재발 주의] 원익IPS·테스 티커 = `.KQ`(코스닥) 고정**: `.KS`(코스피)로 조회하면 다른 종목 시세(22,000/36,750원대)가 튀어나옴 — 6/14에 `market_data.py`·`kr-market-desk.md`·`semi-ai-desk.md`에 `.KQ`로 정정 완료(원익IPS 240810.KQ·테스 095610.KQ). **7/1 EXEC에서 정식 스크립트 대신 별도 조회로 `.KS`를 다시 써서 재발** → 시세 확인은 항상 `market_data.py` 결과를 그대로 쓸 것, 임기응변 재조회 금지.
- **[7/2] "yt-dlp 봇차단 → 로컬 대기"는 오진 (정훈 지적 "회피하지 말고 해결")**: 봇차단의 실체 = 버스트 레이트리밋(일시적). `hunter_latest.py`가 RSS 탐색 + innertube 자막(페이싱 8~15초·백오프 60→240초)으로 **웹 환경에서 정상 작동**(7/2 실측 9/9편 확보, yt-dlp 불요). 신규 영상 미분석 방치 금지·**제목만 로깅(추측 분석) 금지** — 실패 시 `--ids`로 세션 내 재시도. "구독자 전용" 표기 영상도 공개 영상임(자막 정상).
- **[7/2] 외인 수급은 방향만 보지 말고 추세를 본다 (정훈 지시 "매수량도 느는지 확인")**: `flow_trend.py`(streak·5일 강도·전환단계) + `triggers.py` cond=flow(외인 전환·강도축소 자동평가) + KRW=X below 1500 트리거 신설. kr-market-desk가 매일 flows.json 기입(+하닉 `foreign_hynix` 선택 필드).
- **[7/2] EXEC·밤 대화 등 부록 세션도 앱 4파일(stocks·hunter·tasks·flows) 동기화 의무**: v37 아침 서사가 EXEC 폭락 뒤 앱에 남은 사고(시세=폭락/서사=아침 분열) 재발 방지 — validate_report.py가 source_report 파일명 전체 대조로 intra-version stale을 FAIL 적발.
- **[7/11 정훈 확정 — 5시간 롤링 리셋 정렬로 재설계 · 7/16 R4 재시도 추가] 루틴 = 3개 + 재시도 파수꾼 2개 + 밤 대화**: 정본 `docs/routines.md`. **R1 영상 프리페치(평일 10:00)** = 무거운 3채널 영상 자막 분석 전용, 별도 리셋 창에 격리 → 캐시 저장 / **R2 메인 풀 보고서(평일 16:00)** = 신선 창에서 하루 1회 종합, R1 캐시 소비(영상 재추출 X)·전일 밤 미국 체결·밤 지표 반영·완료 후 17:30 폰창 대기 / **R3 주말 캘리브레이션(토 09:00)** = self-review 콜 후행검증 / **R4 토큰-리셋 재시도(평일 18:30·21:15)** = R2가 5시간 롤링 토큰 한도에 막혀 보고서를 못 냈으면 리셋된 신선 창에서 자동 완주(`report_guard.py --check` = 이미 났으면 ~0토큰 즉시 종료, 안 났을 때만 풀 보고서). **폐지**: 구 06:00 풀브리핑(→16:00 이동)·구 17:00 EXEC 전용(→16:00 오더북+17:30 대화 흡수)·구 야간. 근거 = 토큰 한도가 5시간 롤링 리셋 → 영상(무겁고 시간무관)을 6시간 앞 창으로 빼 메인이 신선 예산에서 완주, 그래도 막히면 R4가 리셋 후 재시도. **밤 = 대화형 최종정리**(quick-check·새 버전 금지·부록명 `_night`).
- **[7/2] 외부 채널 영상 = youtube-watch 스킬 / 경제사냥꾼 = hunter_latest.py 구분**: `hunter_latest.py`는 경제사냥꾼 채널ID 고정이라 외부 채널(지식인사이드 등)에 쓰지 말 것 → `fetch_youtube.py`(yt-dlp)로. 웹 환경에선 **`SSL_CERT_FILE=/root/.ccr/ca-bundle.crt` 지정 시 yt-dlp 정상 작동**(7/2 실측 3/3편) — "봇차단" 표기 전에 CA 번들 환경변수부터 확인. 외부 영상 공부 기록 = `docs/research/study_log.md`(hunter_log와 분리).
- **[6/21] 국장 '소액 분수주' 계획 금지 (정훈 지적)**: 토스는 **국내주식 소수점(분수주) 미지원 — 미국주식만 소수점**(master.md §토스거래). 따라서 **국장 매수 계획은 반드시 1주 가격 이상**으로 잡는다. 예: NAVER 1주 ≈229,500원인데 "소액 10만원 매수"는 1주도 못 사 **성립 불가** → v22 보고서·portfolio.json·stocks.json의 "소액 10만/10~15만"을 **"정수 1주 지정가"**로 영구 교정. 국내 매수존 액션엔 항상 '정수 N주(≈가격) 지정가'로 표기, 분수 금액 금지. (미장만 소수점·소수점 예약 가능 — 룰6.)

- **★[8/12 신설 — 같은 필드를 쓰는 쪽과 읽는 쪽이 갈리면 데이터는 조용히 사라진다]** 경제사냥꾼 아카이브 454건 중 **263건이 '미확인'**으로 집계돼 채널 정확도가 **35%**로 표시됐는데, 실제 원인은 검증 부족이 아니라 **배선 유실**이었다. `build_app_data`의 롤오버가 `lv.get("verdict")`를 읽는데 기입 필드는 **`tag`**였다 — 바로 옆 `takeaway`는 `summary or note` 폴백이 이미 있었는데(舊 사고 수정분) **verdict만 안 고쳐 같은 결함이 옆에 남아 있었다.** 회수 결과 **208건이 되살아나 커버리지 47%→91%**. **재발방지**: ①필드명 폴백을 고칠 땐 **같은 함수의 다른 필드도 같이 점검**한다(한 줄만 고치면 형제 버그가 남는다) ②`_verdict_of()`처럼 **회수 경로를 함수로 만들어** 기입처가 늘어도 한 곳만 고치게 한다 ③⚠️ `tag`엔 판정과 **티어 라벨('일일브리핑' 등)이 섞여** 있으므로 판정형만 통과시킨다(무조건 폴백하면 지표가 오염된다).
- **★[8/12 신설 — 지표의 분모를 의심하라: '정확도 35%'는 정확도가 아니었다]** 위 스코어카드는 **미채점·일반 콘텐츠(종목콜 없는 교육·원칙론)까지 분모에 넣고** 있었다. 판정이 확정된 것만 분모로 쓰면 방향 적중률은 **84%**이고, 208건을 회수해 표본을 2배로 늘려도 **83~84%로 안 변한다**(= 표본 편향이 아니라 실제 수준). **규율**: 비율 지표를 만들 땐 **분자만큼 분모를 명시**하고, '아직 안 봄'과 '보고도 확인 실패'와 '볼 대상이 아님'을 **한 바구니에 넣지 않는다**(각각 미채점·미확인·일반). ⚠️ 단 **선택 편향은 남는다** — 검증한 건 검증 가능한 것들이라, 미채점분이 같은 품질이라는 보장은 없다. 이 단서를 지표 옆에 항상 붙인다.
- **★[8/12 신설 — 오더는 '낼 수 있는 주문'인지부터 확인한다]** LG전자 익절 지정가 240,000원이 **당일 상한가 236,000원 초과로 접수 거부**됐다(8/11 종가 181,700×1.3, 호가단위 1,000원). 8/1 '분수주는 지정가가 안 걸린다'(AAPL)와 **같은 클래스** — 판단은 맞았는데 **물리적 접수 가능성**을 안 봤다. 8/2 *"오더북에 들어간 것만 집행된다"*의 숨은 전제 = **애초에 들어갈 수 있는 가격일 것.** 기계화 = `validate_report.check_kr_price_band()`(WARN, 기준가·기준일 병기). 밴드는 매일 재계산되는 하루짜리 제약이라 목표가를 낮추지 않는다.
- **★[8/13 정정 — 토스 국내주식도 예약주문 된다]** 舊 "[8/12 확정] 토스 국내주식 예약주문 없음"은 **오기**다. 8/13 16:18 정훈 스크린샷에서 국내 주문내역 '대기 중인 주문'에 **LG전자 1주 판매 240,000원**이 정상 등록돼 있는 것을 확인했고, 8/3 '삼성전자 1주 구매 실패' 이력도 국내 예약주문이 존재해야 남는 기록이다. 미국(애플 $312 대기)과 동일하게 국내도 예약이 걸린다. ⇒ **국내 오더를 "폰창에 사람이 직접 접수"로만 설계하지 말 것** — 예약으로 걸어두면 정규장에 자동 집행된다. 다만 **가격제한폭은 여전히 유효**하다: 정규장 상한 = 전일종가×1.3 / **시간외단일가 상한 = 당일종가×1.1**(둘 다 `validate_report.check_kr_price_band`가 검사). 밴드 밖 지정가는 예약이어도 접수 거부된다(8/12 LG전자 236,000원 초과 사고).
- **★[8/22 신설 — 만든 도구가 '불리는지'는 기계가 본다. 문서에 적힌 건 배선이 아니다]** 정훈 지시 *"다양한 스킬이나 기능들 다 잘 사용되고 있는지"*로 80개 스크립트를 감사한 결과: **`broker_reports.py`가 7/30부터 멀쩡히 돌고 있었는데(60일 89건·LG전자 목표가 250,000원 등 실데이터) 어떤 데스크도 안 읽고 있었다.** CLAUDE.md가 *"증권사 리포트가 그 위에 얹힌다"*고 **선언까지 해둔 채로** — 문서에 적는 것과 에이전트가 부르는 것은 완전히 별개다. `ma_board.py`(정훈 8/4 지시)도 같은 상태였다. ⇒ 둘 다 `kr-market-desk`에 배선 + **`wiring_audit.py` 신설**(selfcheck에 WARN 단계로 연결 — 게이트는 막지 않는다, 신규 도구는 배선 전이 정상이므로).
  **핵심 교훈 3개**: ①**지시층과 코드층은 다르다.** 감사 초판이 둘을 한 바구니에 세었더니 broker_reports가 "다른 스크립트에 이름이 있다"는 이유로 배선됨으로 나와, 잡으려던 구멍을 0건으로 보고했다 — **에이전트·스킬·루틴(에이전트가 읽고 실행하는 문서)에서 0이면 미배선**이고, 라이브러리 import는 데스크가 산출물을 본다는 보장이 아니다. ②**감사 도구는 음성 테스트로 검증한다.** 배선을 임시로 걷어내고(`git stash`) 실제로 잡히는지 확인하지 않았다면 이 도구도 "돌지만 아무것도 못 잡는" 세 번째 사례가 됐을 것이다. ③**"안 불려도 정상"은 이유와 함께 선언한다** — `EXPECTED_UNWIRED`에 일회성 검정·라이브러리·수동 유틸을 이유와 함께 등록한다. 이유를 못 적겠으면 그건 배선이 필요한 것이다.
- **★[8/22 신설 — 도구 생존은 '메타'가 아니라 '실제 쓰는 산출물'로 판정한다]** 정훈이 가져온 외부 자료(Agent Reach·claude-video 와치) 도입 검토 중 실측에서 발견: 웹(데이터센터 IP)에서 **yt-dlp는 죽어 있다**(web·android·ios·tv·mweb 5개 클라이언트 전부 "Sign in to confirm you're not a bot"). 7/2 메모 *"SSL_CERT_FILE 지정 시 정상 작동(3/3편)"*은 재확인 없이 굳은 **stale 서술**이었고, 그 위에 `youtube-watch`(정훈이 임의 유튜브 링크를 던질 때의 경로)가 **yt-dlp 단일 의존**으로 올라가 있었다 — 3중 폴백을 갖춘 `hunter_latest`(R1 루틴, 매일 정상)와 **바로 옆인데 배선이 안 이어져 있었다**(8/12 "쓰는 쪽과 읽는 쪽이 갈리면 데이터는 조용히 사라진다"의 동류). ⇒ `fetch_youtube.py`에 innertube 폴백 연결(`YW_NO_INNERTUBE_FALLBACK`로 상호 재귀 차단) + `api_health.py`에 **yt-dlp 스트림** 항목 신설.
  **핵심 교훈 2개**: ①**메타 성공 ≠ 생존.** 첫 패치를 '메타 실패 시 폴백'으로 걸었더니 발동조차 안 했다 — yt-dlp는 `--ignore-no-formats-error` 때문에 **메타만 통과하고 자막·스트림은 못 가져온다**. 고장 지점은 *우리가 실제로 쓰는 산출물*(자막)이지 접속 여부가 아니다. 헬스체크·폴백 조건은 **최종 산출물 기준**으로 건다. ②**폴백은 '무언가 받았다'가 아니라 '쓸 것을 받았다'로 판정한다.** 폴백 md가 자막 없이 넘어오자 **메타 마크업이 '자막'으로 둔갑**해 통과했다 — "제목만 로깅(추측 분석) 금지"가 자동화 안에서 깨지는 형태다. 가드 없는 폴백은 침묵보다 나쁘다.
- **★[8/23 신설 — 가드가 있어도 서사가 그 앞을 지나간다]** 토스 실현손익 스크린샷 5장을 받아 31건을 원장에 넣고 *"데스크 이전 -77,955원 · **국내주 2건(-288,590원)이 미국주 +$142를 통째로 뒤집었다**"*는 결론을 냈다. 30분 뒤 정훈이 스크롤 나머지 3장을 보내자 그 결론이 무너졌다 — 빠져 있던 **2026-01-19 SK하이닉스 +199,401(+35.7%)·삼성전자 +80,579(+37.6%)** 한 건이 국내주를 **-288,590 → -9,668원**으로, 전체를 **-77,955 → +163,796원**으로 뒤집었다. **문제는 가드가 없었던 게 아니다** — `coverage` 블록을 만들어 "이건 계좌 전체 실적이 아니다"를 데이터에 박고 앱 배너까지 띄운 상태였다. 그런데 **그 경고를 달아둔 채로 그 위에 인과 서사를 세웠다**("국내주가 원인이다"). 8/22 "가드 없는 폴백은 침묵보다 나쁘다"와 다른 실패다 — 여긴 **가드가 있는데 추론이 그 앞을 지나갔다.** **재발방지**: `coverage.complete == False`인 데이터로는 **기술(descriptive)만 하고 인과·원인 서술을 금지**한다("A가 B를 뒤집었다"·"정체는 C다" 금지, "확보분 기준 합계는 X" 까지만). 부분 표본에서 나온 방향은 **누락분이 채워지면 뒤집힐 수 있는 크기인지**를 먼저 따진다 — 이번엔 미확보 1건이 결론을 뒤집기 충분한 크기였다.
- **★[8/23 신설 — 가드는 '있다'가 아니라 '이 사례를 잡는가'로 검증한다]** 8/5에 만든 `check_repealed_rules`(폐기된 룰이 정본에 살아 있는지 감지)가 **7주간 초록불이었는데, 손으로 찾아보니 폐기된 7,500 안전핀이 세 군데에 현행처럼 살아 있었다**(risk-desk.md 절대룰 1번 · quick-check/SKILL.md · kr-market-desk.md). 원인은 룰이 아니라 **탐지기의 사각 두 개**였다: ①정규식 창이 **20자**라 영문 서술 *"매수 안전핀: when 코스피 falls **below 7,500**"*처럼 사이가 벌어진 형태를 못 잡았다(→60자로 확대) ②스캔 목록이 `portfolio-desk/SKILL.md` **하나만 하드코딩**돼 다른 스킬은 애초에 안 봤다(→`.claude/skills/*/SKILL.md` 열거로 전환). 특히 risk-desk는 **같은 파일 아래쪽에 "舊 안전핀은 폐기"라고 적어두고 위쪽 절대룰만 옛 문구**였다 — 8/12 "쓰는 쪽과 읽는 쪽이 갈리면 데이터는 조용히 사라진다"의 문서판. **재발방지**: 가드를 만들거나 고칠 때 **실제로 존재하는 위반 사례를 되돌려 넣어 잡히는지 확인**한다(git stash 음성 테스트). 8/22 교훈("감사 도구는 음성 테스트로 검증한다")을 **신규 도구뿐 아니라 기존 가드의 정기 점검에도** 적용할 것 — 초록불은 위반이 없다는 뜻이 아니라 **탐지기가 그 형태를 안 본다**는 뜻일 수 있다.
- **★[8/12 신설] 당일 지시 채널 = `data/app/session_directive.json`** — 루틴 프롬프트는 에이전트가 못 고치므로(8/6), 정훈의 "오늘만 이렇게" 지시는 이 파일에 적고 **SessionStart 훅이 date==오늘(KST)인 항목만 주입**한다(자동 만료·여러 날치 사전 등록 가능). 첫 사용 = 8/12 "16:00 R2는 새 번호 말고 v73을 디벨롭" → **실제로 R2가 지시대로 수행 확인**.

