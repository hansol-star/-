# 로드맵 — 목표·만들 기능·추적 대상·필요 자료

> **정본 문서.** 정훈 지시[8/21]: *"우리 목표 등 앞으로 만들 기능 및 추적할 내용, 더 쌓이거나 필요한 자료들 그리고 기관들의 자료."*
> 일일 판단은 보고서, 룰은 crash_tf, 결정·아젠다는 master §9·§10, **만들 것·모을 것은 여기.**
> 갱신 규약: 새 결함·공백을 발견하면 그 세션에서 여기 append(상시 승인 8/12). 완료분은 ✅ 표기 후 분기별로 정리.
> ⚠️ **이 문서는 자랑 목록이 아니라 결핍 목록이다**(정훈 7/20: *"만족하지 마, 상업용 최상급이 기준선"*). 가진 것은 §5에 한 줄로만 적고, 본문은 없는 것으로 채운다.

---

## 1. 우리가 뭘 하려는 건가 (목표)

**1인 전속 리서치센터.** 외부 고객 없음 — 산출물의 유일한 평가자는 *정훈의 실제 계좌 수익률*이다.
그래서 목표는 "좋은 분석"이 아니라 **"집행까지 이어져 계좌에 반영되는 판단"**이다.

| 층 | 목표 | 현재 상태 | 가장 큰 결핍 |
|---|---|---|---|
| **A. 데이터** | 모든 수치는 1차 출처 하드넘버 | 미국 강함(EDGAR·FRED), **한국 약함** | **한국은행 ECOS·KRX 미배선 → 한국 매크로·수급이 2차 소스 의존**(§4-1) |
| **B. 판정** | 룰이 기계로 판정되고 사후 채점됨 | 사다리·룰2·룰4는 기계화 | **목표가·별점의 사후 채점이 최근에야 시작**(target_score 8/8) — 역단조 발견은 8/21(§3-2) |
| **C. 집행** | 판단이 오더북에 들어가고 실제 체결됨 | 8/2 배선 가드로 개선 | **오더 '생존' 확인이 없다**(8/21 실사고, §2-1) |
| **D. 학습** | 틀린 것이 기록되고 다음에 반영됨 | 원장 4종 + memory_recall | **원장이 판단 순간에 자동으로 안 뜬다** — 사람이 불러야 나온다(§2-4) |

**성과 기준선**: 함께한 7주(6/13~8/1) **-7.2%**, 손실의 68.7%가 ⭐2 두 종목(현대차·두산로보)에서 발생.
⇒ **우리의 실패 유형은 "좋은 종목을 못 찾은 것"이 아니라 "나쁜 종목을 안 판 것"이다.** 로드맵의 우선순위는 이 진단을 따른다.

---

## 2. 만들 기능 — 우선순위 순

### 🔴 P0 — 손실에 직결됨

**2-1. 오더 생존 가드** `validate_report.check_order_alive()`
- **왜**: 8/21 정훈 실측으로 대기주문 4건 부재 확인. 우리 기록은 *"✅접수완료(8/19 18:49)"* 로 **2거래일 전 스냅샷을 현재 사실로 취급**하고 있었고, PM은 그걸 근거로 *"생존 확인만 하면 됨"* 이라 단정했다.
- **무엇**: orders status에 실측일 필수 병기 → **2거래일 이상 미확인이면 '생존 불명'으로 자동 강등** + 보고서에 재등록 항목으로 노출.
- **연결**: 8/2 *"오더북에 들어간 것만 집행된다"* 는 **그 오더가 아직 거기 있을 때만** 성립. 8/12 *"낼 수 있는 주문인가"* 의 형제 규칙.

**2-2. 여력 구간별 트림 룰** (룰 변경 → 정훈 승인 필요)
- **왜**: `target_score` 413콜 채점에서 **내재여력 구간 역단조** 확인 — `~+10%` 구간 실현 **-14.5%**(보정편향 -14.72%p) vs `+50%~` 구간 +4.5%(-0.24%p). **"목표가 근접"을 방치 사유로 써온 것이 실증적으로 틀렸다.**
- **무엇**: 여력 ±10% 진입 시 자동으로 *트림 오더 또는 재검토 기한* 강제(⭐2 이하에만 걸린 8/2 가드를 **여력 축으로 확장**).
- **선행 조건**: 구간 역단조가 표본·기간을 바꿔도 재현되는지 **§룰검증 3단계(집계→에피소드 분해→시장 간 횡단면)** 통과. 통과 못 하면 룰을 건드리지 않는다(8/5 확립 절차).

**2-3. 통화 익스포저 추적** `fx_exposure.py` (신규)
- **왜**: 8/21 최초 계산 — **달러 자산 71.9% / 원화 28.1%**, 환율 1% 변동 = 총자산 **58,639원**. 원/달러가 3개월 -8.6% 움직이는 동안 **우리는 이 축을 한 번도 측정하지 않았다.** 종목 손익만 보고 환손익을 안 본 것.
- **무엇**: 통화별 자산 비중 + 환율 %ile + **환손익 기여도 분해**(종목 손익 vs 환 손익) 일일 산출. `build_app_data`에 배선해 앱 노출.
- **파생**: 매수 후보 평가에 **통화 축 추가** — 같은 점수면 부족 통화 쪽에 가산(8/21 신한지주 vs UNH 판단에 실제 적용).

### 🟠 P1 — 판정 품질

**2-4. 원장 자동 회수 (판단 순간 주입)**
- **왜**: `memory_recall`은 좋으나 **사람이 불러야 나온다.** 8/21에 삼성 매수 충동을 경고한 `#d40`(7/20 물타기 위반)·`#m20`·`#m24`(no_add→right 2회)는 **PM이 명시적으로 호출했기 때문에** 떴다. 안 불렀으면 안 떴다.
- **무엇**: 보고서 파이프라인에서 **오더·트림·매수 판단이 등장하는 종목마다 자동으로 원장 상위 3건 주입**(틀린 콜·기각안 포함 — 확증편향 방지).

**2-5. 컨센서스 다중소스 교차** `consensus.py --cross`
- **왜**: 8/21 UNH 목표가가 우리 `consensus.py` **$475.23(26명)** vs chartmill **$397.44** 로 **20% 상충** → 여력 확정 불가, 별점 부여 보류. 단일 소스로는 판정을 못 한다.
- **무엇**: 2개 이상 소스 대조 + 괴리 임계 초과 시 `[미확인]` 자동 태깅. **`naver_value`에는 이미 "목표가 stale 의심(크래시 미반영)" 플래그가 있는데 미국 쪽엔 없다** — 그 로직을 미국으로 이식(현대차 716,000·NAVER 330,565가 그 플래그로 걸러졌다).

**2-6. 스크린샷 판독기** `toss_screenshot.py` (신규)
- **왜**: 8/21 정훈 지적 — 스크린샷의 **평가금 ÷ 수량**이 8/21 프리마켓 주당가인데 보고서는 8/20 종가를 썼다. **정훈이 준 자료가 우리 표보다 최신이었다.** 또 *"이번 달 판매수익 72,937원"* ≈ AAPL 1주 차익 72,110원으로 **체결 건수를 역산**할 수 있었다.
- **무엇**: 수량·현금·평가금 파싱 → ①수량/현금 드리프트 대조 ②주당가 역산 후 캐시 종가와 대조 ③실현손익 합계로 체결 검증. **한계 명시**: 보유 화면엔 대기주문이 없다 → 오더 생존은 **주문내역 화면**을 별도 요청(2-1과 연동).

**2-7. EPS 리비전 승격 심사**
- **왜**: 8/21에 **리비전이 실제로 일을 했다** — WMT가 컨센 여력 **+32.8%(38개 하우스)** 로 후보 1위처럼 보였으나 리비전 **-25.3(8곳 중 7곳 하향)** 으로 탈락. META의 stale target 패턴과 동일 구조였다. **컨센 여력만 봤으면 샀다.**
- **무엇**: `--save` 원장이 층화 검증 표본에 도달하면 **측정 전용 → 판정 입력**으로 승격 재논의(정훈 8/1 지시로 현재 룰 미반영). 현재 결손 = Yahoo가 과거 시계열을 안 줘 낙폭 구간 층화가 불가 → **우리가 매일 쌓는 수밖에 없다.**

### 🟡 P2 — 인프라

**2-8. KRX 수급 1차 출처 배선** (§4-2, 미해결)
**2-9. 앱 #plan 화면 stale 가드 확장** — 8/21에 `tasks.json` 최상위 stale 키 4종(today/week/month/todo) 제거. `build_app_data`·`app.js`가 `tasks` 딕셔너리만 읽어 표시 영향은 없었으나, **읽는 쪽이 하나만 바뀌면 즉시 옛 정보가 노출되는 지뢰**였다(8/12 형제버그와 동일 클래스). 남은 생성물에 같은 패턴이 있는지 전수 점검.
**2-10. `git fetch --unshallow`** — validate가 매번 WARN. 얕은 클론이라 `score_calls --backfill`·self-review §1 콜 스냅샷 복원이 **잘린 범위만 본다.** 사후 채점의 표본이 구조적으로 부족한 원인.

---

## 3. 추적할 내용 (열린 질문 — master §10 연동)

기존 13건은 `master.md §10`이 정본. **8/21 신설 3건**:

**3-1. 통화 익스포저 — 달러 71.9%는 의도된 것인가**
원/달러 1,383.94 = **1년 %ile 0.8**(1년 최저권)이나 3년 43.9·5년 64.3으로 **긴 창에선 중간**. 지난 1년이 비정상적 원화 약세였다는 뜻이지 지금이 절대 저점이란 근거는 없다. 원화 강세가 이어지면(3개월 **-8.6%**) 달러 71.9%가 계속 깎인다. **목표 비중을 정할 것인가, 종목 선택의 부산물로 둘 것인가**가 열린 질문.
▸ 재검토 트리거: 매 보고서 `fx_exposure.py`(2-3) · 한은 금통위 · 미 30년물 방향

**3-2. 목표가 낙관 편향의 구간 역단조 — 왜 여력 작은 종목이 더 빠지나**
`~+10%` 구간이 -14.5%인 것은 *"목표가에 근접했다"* 가 실은 *"이미 다 올랐다"* 의 다른 말이었을 가능성. ⭐5의 실현이 **-8.1%로 별점 중 최악**인 것도 같은 방향. **별점·목표가 산정 자체에 상방 편향이 있는지**를 R3에서 층화 검증.
▸ 현재 ±10% 구간 거주자: **NAVER(-0.5%)·현대차(+8.7%)·AAPL(-11.3%)·ORCL(+11.2%)·META(+11.9%)**

**3-3. 주주환원 테마 — '약속'과 '집행'의 분리**
8/21이 표본을 줬다. 같은 날 **삼성전자**는 *90~110조 "예상"·확정은 10월/내년 1월·15조 자사주는 임직원 보상용(소각 아님)*, **신한지주**는 *이미 환원율 50% 달성·53%로 상향·자사주 1.4조 소각 중(상반기 7천억 집행 완료)*. 시간외에서 삼성 -3.0% vs 하이닉스 0.0%로 시장도 갈라 읽었다.
⇒ **밸류업 서사를 인용할 때 '발표'와 '집행'을 반드시 구분**한다. 추적 대상: 삼성 10월말 이사회(30조 현금배당 확정) · 2027년 1월말 이사회(잔여 확정).

---

## 4. 필요한 자료 — 공백이 큰 순서

### 4-1. 🔴 한국은행 ECOS API (최대 공백 · 8/21 작동 확인)
- **왜 공백인가**: 우리는 미국 매크로를 **FRED 하드넘버**로 쓰면서 **한국 매크로는 뉴스·네이버·야후 2차 소스**로 써왔다. 원/달러조차 Yahoo `KRW=X`(중간값)를 쓰는데, **한국의 정본은 한은 매매기준율**이다.
- **확인**: 8/21 실호출 성공 — `ecos.bok.or.kr/api/StatisticSearch/{key}/json/kr/1/N/731Y001/D/{시작}/{끝}/0000001` → `원/미국달러(매매기준율)` 일별. 샘플키로도 응답.
- **주는 것**: 환율(매매기준율 정본)·기준금리·국고채·CPI/PPI·경상수지·통화량·산업생산 + **한은 자체 점도표**(2·5·8·11월).
- **할 일**: 무료 키 발급(ecos.bok.or.kr 인증키 신청) → `macro_data.py`의 한국 축으로 배선. **환경변수 `ECOS_API_KEY`**.

### 4-2. 🟠 KRX 정보데이터시스템 (미해결)
- **왜**: 외인·기관 수급이 `naver_flows` 의존인데 **종목별 조회에 T+1 지연 구조적 결함**(8/15 R3 규명). 게이트②(외인 순매수 재개) 판정의 정확도가 여기 걸려 있다.
- **8/21 시도 결과**: `data.krx.co.kr` POST가 **`LOGOUT` 응답** — 세션/Referer 인증 필요. 미해결.
- **대안**: KRX 오픈API(open.krx.co.kr, 키 발급) 또는 세션 획득 후 재시도.

### 4-3. 🟡 YouTube Data API v3 키 (대기 중)
8/12에 RSS 3채널 동시 404 + yt-dlp 동시 실패로 무인 R1이 조용히 죽을 뻔했다. 현재 폴백 3단으로 돌아가나 **과거 소급 탐색 불가**(스크레이프는 최근 30편만). 무료 일 10,000 units. **8/21 현재도 자막 재시도 3회 실패 중**(`WyYXeEgtcjY` 외 3편).

### 4-4. 🟡 DRAM 실물 호가 (무료 기계판독 부재 — 확정)
FRED 반도체 PPI는 **프록시로 쓰면 안 된다**(메모리 급등기에 오히려 하락, 8/1 확정). TrendForce는 유료. ⇒ **메모리 사이클 서술 시 "실물가 미확인"을 명시하는 현행 규율 유지**가 현실적 답. 포트 최대 팩터(§10-2)의 핵심 입력이 계속 비어 있다는 사실 자체를 기억할 것.

### 4-5. 미배선 기관 소스 (검토 대상)
| 소스 | 주는 것 | 상태 |
|---|---|---|
| 국민연금 기금운용본부 | 국내 최대 기관의 자산배분·의결권 | 미검토 — `dart_disclosure --major`가 5% 보고로 일부 포착 중 |
| 금감원 공매도 종합 | 종목별 공매도 잔고·대차 | 미검토 (`short_borrow.py` 존재 — 커버리지 확인 필요) |
| BIS·IMF·OECD | 글로벌 신용·유동성 사이클 | 미검토 — 하드플로어(S&P 폭풍)의 상위 맥락으로 유용할 수 있음 |
| 한국거래소 투자자별 매매동향 | 4-2와 동일 | 4-2로 통합 |

---

## 5. 이미 갖춘 것 (한 줄 — 자세히는 `docs/data_coverage.md`)

SEC EDGAR(XBRL 전 시계열·FTS 본문검색·8-K 이벤트·Form 4 내부자·13F/13D·13G) · DART(재무·수시공시 3단 분류·임원·5%) · FRED(미 금리·물가·고용·VIX·유가) · Yahoo(시세·재무·컨센·EPS 리비전·OHLC) · 네이버(뉴스·검색트렌드·컨센·재무) · earningscalls.dev(어닝콜 전문) · 한경컨센서스(sell-side 원문) · FMP(미국 5종목 한정).
**자체 분석층**: 낙폭 기저율(36만 일봉·18개 시장) · 폭풍 %ile(vol_gauge 정본) · 피어 횡단 · 기억 회수 · 목표가 사후채점 · 리테일 심리 %ile.

---

## 6. 다음 3개 세션에서 실제로 할 것

1. **ECOS 키 발급 → `macro_data.py` 한국 축 배선** (4-1) — 공백이 가장 크고 난이도는 낮다
2. **`check_order_alive()` 가드** (2-1) — 8/21 실사고 직결, 코드량 적음
3. **`fx_exposure.py`** (2-3) — 71.9% 편중을 매일 보이게
4. R3에서 **구간 역단조 층화 검증** (3-2) → 통과 시에만 2-2 룰 제안

⚠️ **4번은 룰 변경이라 검증 통과 + 정훈 승인 없이는 손대지 않는다.** 1~3번은 도구·배선이라 상시 승인 범위.
